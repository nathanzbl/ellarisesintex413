<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Edit Donation | Ella Rises</title>

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=DM+Serif+Display:wght@400&family=Montserrat:wght@300;400;500;600&display=swap" rel="stylesheet">

  <style>
    :root {
      --er-blue:     #99B7C6;
      --er-lavender: #978EC4;
      --er-cream:    #F9F5EA;
      --er-blush:    #FFD8D1;
      --er-pink:     #F9AFB1;
      --er-sage:     #9AB59D;
      --er-charcoal: #3A3F3B;
      --er-peach:    #F4B89D;
      --er-rose:     #CE325B;
    }

    * { box-sizing: border-box; }

    body {
      margin: 0;
      font-family: 'Montserrat', sans-serif;
      background: var(--er-cream);
      color: var(--er-charcoal);
    }

    .page-wrapper {
      min-height: 100vh;
      padding: 24px;
      display: flex;
      flex-direction: column;
      gap: 24px;
    }

    h1 {
      font-family: 'DM Serif Display', serif;
      font-size: 2rem;
      margin: 0 0 4px 0;
      color: var(--er-lavender);
    }

    .subtitle {
      margin: 0;
      font-size: 0.95rem;
      color: #555;
    }

    .card {
      background: #fff;
      border-radius: 16px;
      box-shadow: 0 6px 18px rgba(0,0,0,0.06);
      padding: 20px 20px 24px;
      max-width: 700px;
    }

    .field-group {
      margin-bottom: 14px;
      display: flex;
      flex-direction: column;
      gap: 4px;
    }

    label {
      font-size: 0.85rem;
      font-weight: 600;
      color: #555;
    }

    input[type="text"],
    input[type="number"],
    input[type="datetime-local"],
    select {
      padding: 8px 10px;
      border-radius: 8px;
      border: 1px solid #ccc;
      font-size: 0.95rem;
      font-family: inherit;
      width: 100%;
    }

    .readonly-text {
      padding: 8px 0;
      font-size: 0.95rem;
    }

    .checkbox-row {
      display: flex;
      align-items: center;
      gap: 8px;
      margin-top: 4px;
    }

    .btn-row {
      margin-top: 16px;
      display: flex;
      gap: 10px;
      flex-wrap: wrap;
    }

    .btn {
      display: inline-block;
      padding: 9px 16px;
      border-radius: 8px;
      border: none;
      cursor: pointer;
      font-size: 0.95rem;
      font-weight: 600;
      font-family: inherit;
      text-decoration: none;
      text-align: center;
    }

    .btn-primary {
      background: var(--er-sage);
      color: #fff;
    }

    .btn-primary:hover {
      background: var(--er-lavender);
    }

    .btn-secondary {
      background: #fff;
      color: var(--er-charcoal);
      border: 1px solid #ccc;
    }

    .btn-secondary:hover {
      border-color: var(--er-lavender);
      color: var(--er-lavender);
    }

    .error-message {
      color: var(--er-rose);
      margin-bottom: 10px;
      font-size: 0.9rem;
      font-weight: 600;
    }

    @media (max-width: 768px) {
      .page-wrapper {
        padding: 16px;
      }
    }
  </style>
</head>
<body>
  <div class="page-wrapper">
    <header>
      <h1>Edit Donation #<%= donation.donationid %></h1>
      <p class="subtitle">
        <strong>Participant:</strong>
        <%= donation.participantname || 'Anonymous' %>
        <% if (donation.participantemail) { %>
          (<%= donation.participantemail %>)
        <% } %>
        <% if (donation.eventname) { %>
          · <strong>Event:</strong> <%= donation.eventname %>
        <% } %>
      </p>
    </header>

    <div class="card">
      <% if (error_message && error_message !== "") { %>
        <div class="error-message"><%= error_message %></div>
      <% } %>

      <form action="/donations/<%= donation.donationid %>/edit" method="post">
        <div class="field-group">
          <label for="donationamount">Amount (USD)</label>
          <input
            type="number"
            step="0.01"
            id="donationamount"
            name="donationamount"
            value="<%= donation.donationamount %>"
            required
          />
        </div>

        <div class="field-group">
          <label for="donationdate">Donation date and time</label>
          <input
            type="datetime-local"
            id="donationdate"
            name="donationdate"
            value="<%= donation.donationdate_local || '' %>"
          />
        </div>

        

        <div class="field-group">
          <label>Anonymous status</label>
          <div class="checkbox-row">
            <input
              type="checkbox"
              id="isanonymous"
              name="isanonymous"
              value="1"
              <%= donation.isanonymous ? 'checked' : '' %>
            />
            <label for="isanonymous">Mark this donation as anonymous</label>
          </div>
        </div>

        <div class="btn-row">
          <button type="submit" class="btn btn-primary">Save changes</button>
          <a href="/donations/view" class="btn btn-secondary">Cancel</a>
        </div>
      </form>
    </div>
  </div>
</body>
</html>
