<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Add Donation | Ella Rises</title>

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=DM+Serif+Display:wght@400&family=Montserrat:wght@300;400;500;600&display=swap" rel="stylesheet">

  <style>
    :root {
      --er-blue:     #99B7C6;
      --er-lavender: #978EC4;
      --er-cream:    #F9F5EA;
      --er-blush:    #FFD8D1;
      --er-pink:     #F9AFB1;
      --er-sage:     #9AB59D;
      --er-charcoal: #3A3F3B;
      --er-peach:    #F4B89D;
    }

    * { box-sizing: border-box; }

    body {
      margin: 0;
      font-family: 'Montserrat', sans-serif;
      background: var(--er-cream);
      color: var(--er-charcoal);
    }

    .page-wrapper {
      min-height: 100vh;
      padding: 24px;
      max-width: 800px;
      margin: 24px auto 40px;
      display: flex;
      flex-direction: column;
      gap: 24px;
    }

    header {
      display: flex;
      justify-content: space-between;
      align-items: baseline;
      gap: 8px;
      flex-wrap: wrap;
    }

    h1 {
      font-family: 'DM Serif Display', serif;
      font-size: 2.1rem;
      margin: 0;
      color: var(--er-lavender);
    }

    .subtitle {
      margin: 4px 0 0;
      font-size: 0.95rem;
      color: #555;
    }

    .card {
      background: linear-gradient(135deg, #ffffff, #fffaf5);
      border-radius: 18px;
      border: 1px solid #f0e2d9;
      box-shadow: 0 10px 24px rgba(0,0,0,0.04);
      padding: 22px 24px 24px;
    }

    form {
      display: flex;
      flex-direction: column;
      gap: 16px;
    }

    .form-row {
      display: flex;
      gap: 16px;
      flex-wrap: wrap;
    }

    .field {
      flex: 1 1 220px;
      display: flex;
      flex-direction: column;
      gap: 4px;
    }

    label {
      font-size: 0.82rem;
      font-weight: 600;
      color: #666;
      text-transform: uppercase;
      letter-spacing: 0.04em;
    }

    input,
    select {
      padding: 8px 11px;
      border-radius: 9px;
      border: 1px solid #d6d6d6;
      font-size: 0.95rem;
      font-family: inherit;
      background-color: #fff;
      outline: none;
      transition: border-color 0.15s ease, box-shadow 0.15s ease;
    }

    input:focus,
    select:focus {
      border-color: var(--er-lavender);
      box-shadow: 0 0 0 2px rgba(151,142,196,0.18);
    }

    .checkbox-row {
      display: flex;
      align-items: center;
      gap: 8px;
      margin-top: 4px;
    }

    .checkbox-row input[type="checkbox"] {
      width: 18px;
      height: 18px;
    }

    .form-actions {
      margin-top: 8px;
      display: flex;
      justify-content: flex-end;
      gap: 10px;
      flex-wrap: wrap;
    }

    .btn {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      padding: 9px 16px;
      border-radius: 999px;
      border: none;
      cursor: pointer;
      font-size: 0.9rem;
      font-weight: 600;
      font-family: inherit;
      text-decoration: none;
      text-align: center;
      line-height: 1.1;
      transition: background-color 0.15s ease, color 0.15s ease,
                  box-shadow 0.15s ease, transform 0.05s ease;
    }

    .btn-primary {
      background: var(--er-lavender);
      color: #fff;
      box-shadow: 0 6px 14px rgba(151,142,196,0.35);
    }

    .btn-primary:hover {
      background: #7c73c1;
      transform: translateY(-1px);
      box-shadow: 0 8px 18px rgba(151,142,196,0.45);
    }

    .btn-secondary {
      background: #fff;
      color: var(--er-charcoal);
      border: 1px solid #d0d0d0;
    }

    .btn-secondary:hover {
      border-color: var(--er-lavender);
      color: var(--er-lavender);
      background: #faf7ff;
    }

    .error-message {
      margin-top: 8px;
      font-size: 0.85rem;
      color: #b3261e;
    }
  </style>
</head>
<body>
  <%- include('navbar.ejs') %>

  <div class="page-wrapper">
    <header>
      <div>
        <h1>Add Donation</h1>
        <p class="subtitle">Record a new donation for an existing participant and event.</p>
      </div>

      <a href="/donations" class="btn btn-secondary">Back to donations</a>
    </header>

    <div class="card">
      <% if (error_message) { %>
        <p class="error-message"><%= error_message %></p>
      <% } %>

      <form method="post" action="/donations/new">
        <div class="form-row">
          <div class="field">
            <label for="participantid">Participant</label>
            <select id="participantid" name="participantid" required>
              <option value="">Select participant</option>
              <% participants.forEach(function(p) { %>
                <option value="<%= p.participantid %>">
                  <%= p.participantfirstname %> <%= p.participantlastname %> (<%= p.participantemail %>)
                </option>
              <% }); %>
            </select>
          </div>

          <div class="field">
            <label for="eventid">Event</label>
            <select id="eventid" name="eventid" required>
              <option value="">Select event</option>
              <% events.forEach(function(evt) { %>
                <option value="<%= evt.eventid %>">
                  <%= evt.eventname %> – <%= new Date(evt.eventdate).toDateString() %>
                </option>
              <% }); %>
            </select>
          </div>
        </div>

        <div class="form-row">
          <div class="field">
            <label for="donationamount">Amount (USD)</label>
            <input
              type="number"
              step="0.01"
              min="0"
              id="donationamount"
              name="donationamount"
              required
            />
          </div>

          <div class="field">
            <label for="donationdate">Donation date</label>
            <input
              type="date"
              id="donationdate"
              name="donationdate"
              required
            />
          </div>
        </div>

        <div class="field">
          <label>Anonymous</label>
          <div class="checkbox-row">
            <input type="checkbox" id="isanonymous" name="isanonymous" />
            <label for="isanonymous" style="text-transform:none;letter-spacing:0;">
              Hide donor name from public reports
            </label>
          </div>
        </div>

        <div class="form-actions">
          <a href="/donations" class="btn btn-secondary">Cancel</a>
          <button type="submit" class="btn btn-primary">Save donation</button>
        </div>
      </form>
    </div>
  </div>
</body>
</html>
