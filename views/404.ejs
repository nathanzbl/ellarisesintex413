<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>404 | Ella Rises</title>

  <!-- Google Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=DM+Serif+Display:wght@400&family=Montserrat:wght@300;400;500;600&display=swap" rel="stylesheet">

  <style>
    :root {
      --er-blue:     #99B7C6;
      --er-lavender: #978EC4;
      --er-cream:    #F9F5EA;
      --er-blush:    #FFD8D1;
      --er-pink:     #F9AFB1;
      --er-sage:     #9AB59D;
      --er-charcoal: #3A3F3B;
      --er-peach:    #F4B092;
      --er-rose:     #CE325B;
    }

    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 2rem 1rem;
      font-family: "Montserrat", system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      background: var(--er-cream);
      color: var(--er-charcoal);
    }

    .error-card {
      max-width: 640px;
      width: 100%;
      padding: 2.75rem 2.5rem;
      border-radius: 1.75rem;
      background-color: #ffffff;
      box-shadow: 0 14px 40px rgba(0, 0, 0, 0.08);
      border-top: 6px solid var(--er-pink);
      text-align: center;
    }

    .error-code {
      font-family: "DM Serif Display", serif;
      font-size: 3rem;
      letter-spacing: 0.12em;
      margin: 0 0 0.5rem;
    }

    .error-title {
      font-family: "DM Serif Display", serif;
      font-size: 1.7rem;
      margin: 0 0 0.75rem;
    }

    .error-message {
      font-size: 0.98rem;
      margin: 0 0 1.75rem;
      color: rgba(58, 63, 59, 0.8);
    }

    .hint {
      font-size: 0.85rem;
      margin-bottom: 2rem;
      color: rgba(58, 63, 59, 0.7);
    }

    .btn-row {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 0.75rem;
    }

    .btn {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      padding: 0.85rem 1.3rem;
      border-radius: 999px;
      border: none;
      font-weight: 600;
      font-size: 0.9rem;
      letter-spacing: 0.08em;
      text-transform: uppercase;
      cursor: pointer;
      text-decoration: none;
      transition: background-color 0.18s ease, box-shadow 0.18s ease, transform 0.05s ease, color 0.18s ease, border-color 0.18s ease;
    }

    .btn-primary {
      background-color: var(--er-pink);
      color: #ffffff;
      box-shadow: 0 10px 24px rgba(206, 50, 91, 0.25);
    }

    .btn-primary:hover {
      background-color: var(--er-rose);
      box-shadow: 0 12px 30px rgba(206, 50, 91, 0.32);
      transform: translateY(-1px);
    }

    .btn-outline {
      background-color: #ffffff;
      color: var(--er-charcoal);
      border: 1px solid rgba(58, 63, 59, 0.22);
    }

    .btn-outline:hover {
      border-color: var(--er-sage);
      background-color: rgba(154, 181, 157, 0.1);
      transform: translateY(-1px);
    }
  </style>
</head>
<body>
  <div class="error-card">
    <p class="error-code">404</p>
    <h1 class="error-title">Page not found</h1>
    <p class="error-message">
      We could not find the page you were looking for. The link might be broken or the page may have been moved.
    </p>
    <p class="hint">
      Try going back to the home page or return to your previous screen.
    </p>

    <div class="btn-row">
      <a href="/" class="btn btn-primary">Go home</a>
      <a href="javascript:history.back()" class="btn btn-outline">Go back</a>
    </div>
  </div>
</body>
</html>
