<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Participant Profile | Ella Rises</title>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=DM+Serif+Display:wght@400&family=Montserrat:wght@300;400;500;600&display=swap" rel="stylesheet">

    <style>
        :root {
            /* Full Ella Rises Color Palette */
            --er-blue:     #99B7C6;
            --er-lavender: #978EC4;
            --er-cream:    #F9F5EA;
            --er-blush:    #FFD8D1;
            --er-pink:     #F9AFB1;
            --er-sage:     #9AB59D;
            --er-charcoal: #3A3F3B;
            --er-peach:    #F4B092;
            --er-rose:     #CE325B;
        }

        * { box-sizing: border-box; }

        body {
            margin: 0;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 0;
            font-family: "Montserrat", system-ui, sans-serif;
            background: var(--er-cream);
            color: var(--er-charcoal);
        }

        .main-content {
            flex: 1;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 2rem 1rem;
            width: 100%;
        }

        .data-card {
            max-width: 720px;
            width: 100%;
            padding: 2.5rem 2.25rem;
            border-radius: 1.5rem;
            background-color: #ffffff;
            box-shadow: 0 14px 40px rgba(0, 0, 0, 0.08);
            border-top: 6px solid var(--er-pink);
        }

        .milestone-title {
            font-family: "DM Serif Display", serif;
            font-size: 2rem;
            text-align: center;
            letter-spacing: 0.06em;
            margin: 0 0 0.25rem;
        }

        .milestone-subtitle {
            font-size: 0.95rem;
            text-align: center;
            color: rgba(58, 63, 59, 0.7);
            margin-bottom: 2rem;
        }
        
        /* FORM-SPECIFIC STYLES */
        .mb-3 {
            margin-bottom: 2rem;
        }

        /* NEW: Flex container for City/State/Zip */
        .address-group {
            display: grid;
            grid-template-columns: 1fr 1fr; /* Default to 2 columns for medium screens */
            gap: 1.5rem;
            align-items: start; /* Align all items to the top */
        }

        /* Ensure consistent label heights */
        .address-group .field-label {
            min-height: 1.5rem;
            display: flex;
            justify-content: space-between;
            align-items: baseline;
            white-space: nowrap; /* Prevent wrapping */
        }

        /* NEW: Stack City/State/Zip on small screens */
        @media (min-width: 600px) {
            .address-group {
                grid-template-columns: 3fr 2fr 1.5fr; /* City | State | Zip */
            }
        }


        .field-label {
            display: flex;
            justify-content: space-between;
            align-items: baseline;
            margin-bottom: 0.75rem;
            font-size: 0.85rem;
            font-weight: 600;
            letter-spacing: 0.08em;
            text-transform: uppercase;
        }

        .text-input,
        .date-input,
        .select-input {
            width: 100%;
            padding: 1rem;
            border: 2px solid var(--er-blush);
            border-radius: 0.75rem;
            font-family: inherit;
            font-size: 0.9rem;
            outline: none;
            transition: border-color 0.2s ease;
            background: #fff;
        }

        .text-input:focus,
        .date-input:focus,
        .select-input:focus {
            border-color: var(--er-pink);
        }

        .text-input::placeholder,
        .date-input::placeholder {
            color: rgba(58, 63, 59, 0.5);
        }

        input[type="date"]::-webkit-calendar-picker-indicator {
            filter: invert(30%) sepia(0%) saturate(0%) hue-rotate(0deg) brightness(50%);
            cursor: pointer;
        }


        /* BUTTON STYLING*/
        .btn-action {
            padding: 0.85rem 1rem;
            border-radius: 999px;
            border: none;
            font-weight: 600;
            font-size: 0.95rem;
            letter-spacing: 0.08em;
            text-transform: uppercase;
            cursor: pointer;
            box-shadow: 0 10px 24px rgba(206, 50, 91, 0.25);
            transition: all 0.18s ease;
            text-decoration: none;
            display: inline-block;
            text-align: center;
        }

        .btn-save-milestone {
            background: var(--er-pink);
            color: #fff;
            box-shadow: 0 4px 12px rgba(249, 175, 177, 0.3);
        }

        .btn-save-milestone:hover {
            background: var(--er-rose);
            transform: translateY(-2px);
            box-shadow: 0 6px 16px rgba(206, 50, 91, 0.4);
        }

        .btn-back {
            background: var(--er-charcoal);
            color: #fff;
            margin-left: 1rem;
            box-shadow: 0 4px 12px rgba(58, 63, 59, 0.3);
        }

        .btn-back:hover {
            background: rgba(58, 63, 59, 0.9);
            transform: translateY(-2px);
            box-shadow: 0 6px 16px rgba(58, 63, 59, 0.4);
        }

        .message-alert {
            text-align: center;
            color: var(--er-rose);
            background: rgba(206, 50, 91, 0.07);
            padding: 0.8rem 1.2rem;
            border-radius: 0.75rem;
            margin-bottom: 1.5rem;
        }
        
        .text-center-wrapper {
             text-align: center;
             margin-top: 2rem;
        }

        hr { 
            border: none;
            border-top: 1px solid var(--er-blush);
            margin: 2rem 0;
        }

        /* NEW STYLES FOR MILESTONE TABLE */
        .section-title {
            font-size: 1.5rem;
            font-weight: 600;
            text-align: left;
            margin-top: 2rem;
            margin-bottom: 1.25rem;
            color: var(--er-rose);
        }

        .milestone-table-custom {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 2rem; /* Add spacing below the table */
        }

        .milestone-table-custom thead tr {
            background-color: var(--er-blue);
            color: var(--er-cream);
            font-size: 0.75rem;
            letter-spacing: 0.08em;
            text-transform: uppercase;
        }

        .milestone-table-custom th,
        .milestone-table-custom td {
            padding: 0.75rem 1rem;
            text-align: left;
            border-bottom: 1px solid var(--er-blush);
            font-size: 0.9rem;
        }

        .milestone-table-custom tbody tr:nth-child(even) {
            background-color: var(--er-cream);
        }
    </style>
</head>
<body>
    <%- include('../common/navbar.ejs') %>

    <div class="main-content">
        <div class="data-card">
            
            <h1 class="milestone-title">Participant Profile</h1>
            <p class="milestone-subtitle">View and update the primary details for this participant.</p>
            
           
            <% if (typeof error_message !== 'undefined' && error_message) { %>
                <div class="message-alert" style="color: var(--er-rose); background: rgba(206, 50, 91, 0.07);">
                    <%= error_message %>
                </div>
            <% } %>

            <form action="/editParticipant/<%= participant.participantid %>" method="post">
                
                <h2 class="section-title" style="color: var(--er-charcoal);">Primary Details</h2>

                <!-- ID is typically static for display -->
                <div class="mb-3">
                    <label for="participantId" class="field-label">Participant ID:</label>
                    <input type="text" 
                           class="text-input" 
                           id="participantId"
                           value="<%= participant.participantid %>" 
                           readonly 
                           style="background-color: var(--er-cream); opacity: 0.8;">
                </div>

                <div class="mb-3">
                    <label for="firstName" class="field-label">First Name:</label>
                    <input type="text" 
                           class="text-input" 
                           id="firstName" 
                           name="firstName" 
                           value="<%= participant.participantfirstname %>" 
                           required>
                </div>
                
                <div class="mb-3">
                    <label for="lastName" class="field-label">Last Name:</label>
                    <input type="text" 
                           class="text-input" 
                           id="lastName" 
                           name="lastName" 
                           value="<%= participant.participantlastname %>" 
                           required>
                </div>

                <div class="mb-3">
                    <label for="email" class="field-label">Email:</label>
                    <input type="email" 
                           class="text-input" 
                           id="email" 
                           name="email" 
                           value="<%= participant.participantemail %>" 
                           required>
                </div>
                
                <!-- NEW: Phone Number -->
                <div class="mb-3">
                    <label for="phone" class="field-label">
                        <span>Phone Number</span>
                        <span style="font-weight: 400; font-size: 0.7rem; color: #999;">Optional</span>
                    </label>
                    <input type="tel"
                           class="text-input"
                           id="phone"
                           name="phone"
                           value="<%= participant.participantphone || '' %>"
                           pattern="[\(]?[0-9]{3}[\)]?[\s\-]?[0-9]{3}[\s\-]?[0-9]{4}"
                           title="Enter a valid phone number (e.g., 555-555-5555 or (555) 555-5555)"
                           placeholder="e.g., (555) 555-5555 or 555-555-5555">
                </div>

                <!-- NEW: City, State, Zip (Grouped for Layout) -->
                <div class="address-group mb-3">

                    <div>
                        <label for="city" class="field-label">
                            <span>City</span>
                            <span style="font-weight: 400; font-size: 0.7rem; color: #999;">Optional</span>
                        </label>
                        <input type="text"
                               class="text-input"
                               id="city"
                               name="city"
                               value="<%= participant.participantcity || '' %>"
                               placeholder="e.g., Austin">
                    </div>

                    <div>
                        <label for="state" class="field-label">
                            <span>State</span>
                            <span style="font-weight: 400; font-size: 0.7rem; color: #999;">Optional</span>
                        </label>
                        <input type="text"
                               class="text-input"
                               id="state"
                               name="state"
                               value="<%= participant.participantstate || '' %>"
                               maxlength="2"
                               pattern="[A-Za-z]{2}"
                               title="Enter 2-letter state code (e.g., TX, CA, NY)"
                               placeholder="e.g., TX">
                    </div>

                    <div>
                        <label for="zip" class="field-label">
                            <span>Zip Code</span>
                            <span style="font-weight: 400; font-size: 0.7rem; color: #999;">Optional</span>
                        </label>
                        <input type="text"
                               class="text-input"
                               id="zip"
                               name="zip"
                               value="<%= participant.participantzip || '' %>"
                               pattern="[0-9]{5}(-[0-9]{4})?"
                               title="Enter 5-digit zip code or 9-digit zip+4 (e.g., 12345 or 12345-6789)"
                               placeholder="e.g., 12345">
                    </div>
                    </div>

                                    <div class="mb-3">
                                        <label for="dob" class="field-label">Date of Birth:</label>
                                        <input type="date" 
                                               class="date-input" 
                                               id="dob" 
                                               name="dob" 
                                               value="<%= participant.participantdob ? new Date(participant.participantdob).toISOString().split('T')[0] : '' %>">
                                    </div>

                                    <div class="mb-3">
                                        <label for="schoolOrEmployer" class="field-label">School or Employer:</label>
                                        <input type="text" 
                                               class="text-input" 
                                               id="schoolOrEmployer" 
                                               name="schoolOrEmployer" 
                                               value="<%= participant.participantschooloremployer || '' %>" 
                                               placeholder="Current school or place of employment">
                                    </div>

                                    <div class="mb-3">
                                        <label for="fieldOfInterest" class="field-label">Field of Interest:</label>
                                        <input type="text" 
                                               class="text-input" 
                                               id="fieldOfInterest" 
                                               name="fieldOfInterest" 
                                               value="<%= participant.participantfieldofinterest || '' %>" 
                                               placeholder="e.g., Healthcare, Technology, Education">
                                    </div>

                                    <div class="mb-3">
                                        <label for="totalDonations" class="field-label">Total Donation dollar amount:</label>
                                        <input type="number" 
                                               class="text-input" 
                                               id="totalDonations" 
                                               name="totalDonations" 
                                               value="<%= participant.totaldonations || 0 %>" 
                                               step="0.01"
                                               min="0"
                                               style="background-color: var(--er-cream); opacity: 0.8;"
                                               readonly>
                                    </div>

                
                <h2 class="section-title">Milestone History</h2>
                
                <% // Check if the milestones array is defined and has content
                if (typeof milestones === 'undefined' || milestones.length === 0) { %>
                    <div class="message-alert" style="color: var(--er-charcoal); background: var(--er-blush); border: 1px solid var(--er-pink); margin-bottom: 2rem;">
                        This participant has no recorded milestone history.
                    </div>
                <% } else { %>
                    <table class="milestone-table-custom">
                        <thead>
                            <tr>
                                <th>Milestone Title</th>
                                <th>Date Achieved</th>
                                <th>Edit Milestone</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% for (let i = 0; i < milestones.length; i++) { %>
                                <tr>
                                    <td><%= milestones[i].milestonetitle %></td>
                                    <td><%= milestones[i].milestonedate %></td>
                                    <td>
                                        <a href="/editMilestone/<%= milestones[i].milestoneid %>" class="btn-action btn-save-milestone" style="padding: 0.5rem 0.75rem; font-size: 0.8rem;">Edit</a>
                                    </td>
                                </tr>
                            <% } %>
                        </tbody>
                    </table>
                <% } %>
                <hr>

                <div class="text-center-wrapper">
                    <input type="submit" value="Update Details" class="btn-action btn-save-milestone">
                    <a href="/participants" class="btn-action btn-back">Back to Participants</a>
                </div>

                
            
        </div>
    </div>
    <%- include('../common/footer.ejs') %>

</body>
</html>