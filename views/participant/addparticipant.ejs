<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title><%= isPublic ? 'Register' : 'Add Participant' %> | Ella Rises</title>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=DM+Serif+Display:wght@400&family=Montserrat:wght@300;400;500;600&display=swap" rel="stylesheet">

    <style>
        :root {
            /* Full Ella Rises Color Palette */
            --er-blue:     #99B7C6;
            --er-lavender: #978EC4;
            --er-cream:    #F9F5EA;
            --er-blush:    #FFD8D1;
            --er-pink:     #F9AFB1;
            --er-sage:     #9AB59D;
            --er-charcoal: #3A3F3B;
            --er-peach:    #F4B092;
            --er-rose:     #CE325B;
        }

        * { box-sizing: border-box; }

        body {
            margin: 0;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 0;
            font-family: "Montserrat", system-ui, sans-serif;
            background: var(--er-cream);
            color: var(--er-charcoal);
        }

        .main-content {
            flex: 1;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 2rem 1rem;
            width: 100%;
        }

        .main-content.public {
            align-items: flex-start;
            padding-top: 3rem;
        }

        .data-card {
            max-width: 720px;
            width: 100%;
            padding: 2.5rem 2.25rem;
            border-radius: 1.5rem;
            background-color: #ffffff;
            box-shadow: 0 14px 40px rgba(0, 0, 0, 0.08);
            border-top: 6px solid var(--er-pink);
        }

        .milestone-title {
            font-family: "DM Serif Display", serif;
            font-size: 2rem;
            text-align: center;
            letter-spacing: 0.06em;
            margin: 0 0 0.25rem;
        }

        .milestone-subtitle {
            font-size: 0.95rem;
            text-align: center;
            color: rgba(58, 63, 59, 0.7);
            margin-bottom: 2rem;
        }
        
        /* FORM-SPECIFIC STYLES */
        .mb-3 {
            margin-bottom: 2rem;
        }

        .field-label {
            display: flex;
            justify-content: space-between;
            align-items: baseline;
            margin-bottom: 0.75rem;
            font-size: 0.85rem;
            font-weight: 600;
            letter-spacing: 0.08em;
            text-transform: uppercase;
        }

        .text-input,
        .date-input,
        .select-input {
            width: 100%;
            padding: 1rem;
            border: 2px solid var(--er-blush);
            border-radius: 0.75rem;
            font-family: inherit;
            font-size: 0.9rem;
            outline: none;
            transition: border-color 0.2s ease;
            background: #fff;
        }

        .text-input:focus,
        .date-input:focus,
        .select-input:focus {
            border-color: var(--er-pink);
        }

        .text-input::placeholder,
        .date-input::placeholder {
            color: rgba(58, 63, 59, 0.5);
        }

        input[type="date"]::-webkit-calendar-picker-indicator {
            filter: invert(30%) sepia(0%) saturate(0%) hue-rotate(0deg) brightness(50%);
            cursor: pointer;
        }


        /* BUTTON STYLING */
        .btn-action {
            padding: 0.85rem 1rem;
            border-radius: 999px;
            border: none;
            font-weight: 600;
            font-size: 0.95rem;
            letter-spacing: 0.08em;
            text-transform: uppercase;
            cursor: pointer;
            box-shadow: 0 10px 24px rgba(206, 50, 91, 0.25);
            transition: all 0.18s ease;
            text-decoration: none;
            display: inline-block;
            text-align: center;
        }

        .btn-save-milestone {
            background: var(--er-pink);
            color: #fff;
        }
        
        .btn-save-milestone:hover {
            background: var(--er-rose);
            transform: translateY(-1px);
        }

        .btn-back {
            background: var(--er-charcoal);
            color: #fff;
            margin-left: 1rem;
        }
        
        .btn-back:hover {
            background: rgba(58, 63, 59, 0.8);
            transform: translateY(-1px);
        }

        .message-alert {
            text-align: center;
            color: var(--er-rose);
            background: rgba(206, 50, 91, 0.07);
            padding: 0.8rem 1.2rem;
            border-radius: 0.75rem;
            margin-bottom: 1.5rem;
        }
        
        .text-center-wrapper {
             text-align: center;
             margin-top: 2rem;
        }

        hr {
            border: none;
            border-top: 1px solid var(--er-blush);
            margin: 2rem 0;
        }

        .section-divider {
            font-size: 1.2rem;
            font-weight: 600;
            color: var(--er-charcoal);
            margin: 2rem 0 1rem;
        }
    </style>
</head>
<body>
    <% if (!isPublic) { %>
        <%- include('../common/navbar.ejs') %>
    <% } %>

    <div class="main-content <%= isPublic ? 'public' : '' %>">
        <div class="data-card">

            <h1 class="milestone-title"><%= isPublic ? 'Create Your Account' : 'Add New Participant' %></h1>
            <p class="milestone-subtitle"><%= isPublic ? 'Register to join the Ella Rises community.' : 'Enter all required data for the new participant.' %></p>
            
            <% if (error_message) { %>
                <div class="message-alert" style="color: var(--er-rose); background: rgba(206, 50, 91, 0.07);">
                    <%= error_message %>
                 </div>
            <% } %>

            <form action="<%= isPublic ? '/addParticipantPublic' : '/addParticipant' %>" method="post">

                <% if (isPublic) { %>
                    <div class="section-divider">Account Information</div>

                    <div class="mb-3">
                        <label for="username" class="field-label">Username:</label>
                        <input
                            type="text"
                            class="text-input"
                            id="username"
                            name="username"
                            placeholder="Choose a username"
                            required
                        >
                    </div>

                    <div class="mb-3">
                        <label for="password" class="field-label">Password:</label>
                        <input
                            type="password"
                            class="text-input"
                            id="password"
                            name="password"
                            placeholder="Choose a secure password"
                            required
                        >
                    </div>

                    <div class="section-divider">Personal Information</div>
                <% } %>

                <div class="mb-3">
                    <label for="firstName" class="field-label">First Name:</label>
                    <input
                        type="text"
                        class="text-input"
                        id="firstName"
                        name="firstName"
                        placeholder="e.g., Jane"
                        required
                    >
                </div>

                <div class="mb-3">
                    <label for="lastName" class="field-label">Last Name:</label>
                    <input type="text" class="text-input" id="lastName" name="lastName" placeholder="e.g., Doe" required>
                </div>

                <div class="mb-3">
                    <label for="email" class="field-label">Email:</label>
                    <input type="email" class="text-input" id="email" name="email" placeholder="e.g., jane.doe@example.com" required>
                </div>

                <div class="mb-3">
                    <label for="phone" class="field-label">
                        <span>Phone Number</span>
                        <span style="font-weight: 400; font-size: 0.7rem; color: #999;">Optional</span>
                    </label>
                    <input type="tel"
                           class="text-input"
                           id="phone"
                           name="phone"
                           pattern="[\(]?[0-9]{3}[\)]?[\s\-]?[0-9]{3}[\s\-]?[0-9]{4}"
                           title="Enter a valid phone number (e.g., 555-555-5555 or (555) 555-5555)"
                           placeholder="e.g., (555) 555-5555 or 555-555-5555">
                </div>

                <div class="mb-3">
                    <label for="city" class="field-label">
                        <span>City</span>
                        <span style="font-weight: 400; font-size: 0.7rem; color: #999;">Optional</span>
                    </label>
                    <input type="text"
                           class="text-input"
                           id="city"
                           name="city"
                           placeholder="e.g., Austin">
                </div>

                <div class="mb-3">
                    <label for="state" class="field-label">
                        <span>State</span>
                        <span style="font-weight: 400; font-size: 0.7rem; color: #999;">Optional</span>
                    </label>
                    <input type="text"
                           class="text-input"
                           id="state"
                           name="state"
                           maxlength="2"
                           pattern="[A-Za-z]{2}"
                           title="Enter 2-letter state code (e.g., TX, CA, NY)"
                           placeholder="e.g., TX">
                </div>

                <div class="mb-3">
                    <label for="zip" class="field-label">
                        <span>Zip Code</span>
                        <span style="font-weight: 400; font-size: 0.7rem; color: #999;">Optional</span>
                    </label>
                    <input type="text"
                           class="text-input"
                           id="zip"
                           name="zip"
                           pattern="[0-9]{5}(-[0-9]{4})?"
                           title="Enter 5-digit zip code or 9-digit zip+4 (e.g., 12345 or 12345-6789)"
                           placeholder="e.g., 12345">
                </div>

                <div class="mb-3">
                    <label for="dob" class="field-label">
                        <span>Date of Birth</span>
                        <span style="font-weight: 400; font-size: 0.7rem; color: #999;">Optional</span>
                    </label>
                    <input type="date"
                           class="date-input"
                           id="dob"
                           name="dob">
                </div>

                <div class="mb-3">
                    <label for="schoolOrEmployer" class="field-label">
                        <span>School or Employer</span>
                        <span style="font-weight: 400; font-size: 0.7rem; color: #999;">Optional</span>
                    </label>
                    <input type="text"
                           class="text-input"
                           id="schoolOrEmployer"
                           name="schoolOrEmployer"
                           placeholder="Current school or place of employment">
                </div>

                <div class="mb-3">
                    <label for="fieldOfInterest" class="field-label">
                        <span>Field of Interest</span>
                        <span style="font-weight: 400; font-size: 0.7rem; color: #999;">Optional</span>
                    </label>
                    <input type="text"
                           class="text-input"
                           id="fieldOfInterest"
                           name="fieldOfInterest"
                           placeholder="e.g., Healthcare, Technology, Education">
                </div>

                <hr>

                <div class="text-center-wrapper">
                    <input type="submit" value="<%= isPublic ? 'Create Account' : 'Save Participant' %>" class="btn-action btn-save-milestone">
                    <a href="<%= isPublic ? '/login' : '/participants' %>" class="btn-action btn-back"><%= isPublic ? 'Back to Login' : 'Back to Participants' %></a>
                </div>

            </form>

        </div>
    </div>
    <%- include('../common/footer.ejs') %>

</body>
</html>