<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Add User | Ella Rises</title>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=DM+Serif+Display:wght@400&family=Montserrat:wght@300;400;500;600&display=swap" rel="stylesheet">

    <style>
        :root {
            --er-blue:     #99B7C6;
            --er-lavender: #978EC4;
            --er-cream:    #F9F5EA;
            --er-blush:    #FFD8D1;
            --er-pink:     #F9AFB1;
            --er-sage:     #9AB59D;
            --er-charcoal: #3A3F3B;
            --er-peach:    #F4B092;
            --er-rose:     #CE325B;
        }

        * { box-sizing: border-box; }

        body {
            margin: 0;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 0;
            font-family: "Montserrat", system-ui, sans-serif;
            background: var(--er-cream);
            color: var(--er-charcoal);
        }

        .main-content {
            flex: 1;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 2rem 1rem;
            width: 100%;
        }

        .data-card {
            max-width: 900px;
            width: 100%;
            padding: 2.5rem 2.25rem;
            border-radius: 1.5rem;
            background-color: #ffffff;
            box-shadow: 0 14px 40px rgba(0, 0, 0, 0.08);
            border-top: 6px solid var(--er-pink);
        }

        .milestone-title {
            font-family: "DM Serif Display", serif;
            font-size: 2rem;
            text-align: center;
            letter-spacing: 0.06em;
            margin: 0 0 0.25rem;
        }

        .milestone-subtitle {
            font-size: 0.95rem;
            text-align: center;
            color: rgba(58, 63, 59, 0.7);
            margin-bottom: 2rem;
        }

        .card-meta {
            font-size: 0.85rem;
            color: rgba(58, 63, 59, 0.7);
            text-align: center;
            margin-bottom: 2rem;
            padding: 0.75rem 1rem;
            background: rgba(153, 183, 198, 0.12);
            border-radius: 0.75rem;
        }

        .message-alert {
            text-align: center;
            color: var(--er-rose);
            background: rgba(206, 50, 91, 0.07);
            padding: 0.8rem 1.2rem;
            border-radius: 0.75rem;
            margin-bottom: 1.5rem;
        }

        form.add-user-form {
            display: grid;
            grid-template-columns: 1fr;
            gap: 1.5rem;
        }

        .form-section {
            grid-column: 1 / -1;
            display: grid;
            grid-template-columns: repeat(2, minmax(0, 1fr));
            gap: 1.5rem 1.25rem;
            padding: 1.5rem 1.75rem;
            border-radius: 1rem;
            background: rgba(249, 175, 177, 0.08);
            border: 1px solid var(--er-blush);
        }

        .form-section-title {
            grid-column: 1 / -1;
            font-size: 0.85rem;
            text-transform: uppercase;
            letter-spacing: 0.12em;
            font-weight: 600;
            color: var(--er-charcoal);
            margin-bottom: 0.25rem;
        }

        .form-section-helper {
            grid-column: 1 / -1;
            font-size: 0.8rem;
            color: rgba(58, 63, 59, 0.65);
            margin-bottom: 0.5rem;
        }

        .form-group {
            display: flex;
            flex-direction: column;
            gap: 0.5rem;
        }

        label {
            font-size: 0.85rem;
            font-weight: 600;
            letter-spacing: 0.05em;
            text-transform: uppercase;
            color: var(--er-charcoal);
        }

        .required-indicator {
            color: var(--er-rose);
            margin-left: 2px;
        }

        input[type="text"],
        input[type="email"],
        input[type="password"],
        input[type="date"],
        select {
            padding: 1rem;
            border: 2px solid var(--er-blush);
            border-radius: 0.75rem;
            font-size: 0.9rem;
            font-family: inherit;
            outline: none;
            background-color: #fff;
            transition: border-color 0.2s ease;
        }

        input[type="text"]:focus,
        input[type="email"]:focus,
        input[type="password"]:focus,
        input[type="date"]:focus,
        select:focus {
            border-color: var(--er-pink);
        }

        input::placeholder {
            color: rgba(58, 63, 59, 0.5);
        }

        select {
            appearance: none;
            background-image: url("data:image/svg+xml,%3Csvg fill='none' stroke='%233A3F3B' stroke-width='1.5' viewBox='0 0 24 24' xmlns='http://www.w3.org/2000/svg'%3E%3Cpath stroke-linecap='round' stroke-linejoin='round' d='m6 9 6 6 6-6'%3E%3C/path%3E%3C/svg%3E");
            background-repeat: no-repeat;
            background-position: right 12px center;
            background-size: 18px 18px;
            padding-right: 40px;
        }

        .field-hint {
            font-size: 0.78rem;
            color: rgba(58, 63, 59, 0.65);
            font-weight: 400;
            text-transform: none;
            letter-spacing: normal;
        }

        .btn-action {
            padding: 0.85rem 1rem;
            border-radius: 999px;
            border: none;
            font-weight: 600;
            font-size: 0.95rem;
            letter-spacing: 0.08em;
            text-transform: uppercase;
            cursor: pointer;
            box-shadow: 0 10px 24px rgba(206, 50, 91, 0.25);
            transition: all 0.18s ease;
            text-decoration: none;
            display: inline-block;
            text-align: center;
        }

        .btn-save-milestone {
            background: var(--er-pink);
            color: #fff;
            box-shadow: 0 4px 12px rgba(249, 175, 177, 0.3);
        }

        .btn-save-milestone:hover {
            background: var(--er-rose);
            transform: translateY(-2px);
            box-shadow: 0 6px 16px rgba(206, 50, 91, 0.4);
        }

        .btn-back {
            background: var(--er-charcoal);
            color: #fff;
            margin-left: 1rem;
            box-shadow: 0 4px 12px rgba(58, 63, 59, 0.3);
        }

        .btn-back:hover {
            background: rgba(58, 63, 59, 0.9);
            transform: translateY(-2px);
            box-shadow: 0 6px 16px rgba(58, 63, 59, 0.4);
        }

        .text-center-wrapper {
            text-align: center;
            margin-top: 2rem;
        }

        hr {
            border: none;
            border-top: 1px solid var(--er-blush);
            margin: 2rem 0;
        }

        @media (max-width: 768px) {
            .data-card {
                padding: 2rem 1.25rem;
            }

            .milestone-title {
                font-size: 1.7rem;
            }

            .form-section {
                grid-template-columns: 1fr;
            }

            .btn-back {
                margin-left: 0;
                margin-top: 0.5rem;
            }

            .text-center-wrapper {
                display: flex;
                flex-direction: column;
                gap: 0.5rem;
            }
        }
    </style>
</head>
<body>
    <%- include('../common/navbar.ejs') %>
    <%
        var fd = (typeof formData !== "undefined" && formData) ? formData : {};
    %>

    <div class="main-content">
        <div class="data-card">

            <h1 class="milestone-title">Add User</h1>
            <p class="milestone-subtitle">
                Create a user and attach it to a participant. Email, first name, and last name are required.
            </p>

            <div class="card-meta">
                If a participant with this email exists, their record is reused. Otherwise a new participant is created.
            </div>

            <% if (typeof error_message !== "undefined" && error_message) { %>
                <div class="message-alert">
                    <%= error_message %>
                </div>
            <% } %>

            <form class="add-user-form" method="POST" action="/admin/users/add">
                <!-- Participant info section -->
                <div class="form-section">
                    <div class="form-section-title">Participant details</div>
                    <div class="form-section-helper">
                        Email, first name, and last name are required. The rest help reporting and follow up.
                    </div>

                    <div class="form-group">
                        <label for="participantemail">
                            Email
                            <span class="required-indicator">*</span>
                        </label>
                        <input
                            type="email"
                            id="participantemail"
                            name="participantemail"
                            required
                            placeholder="name@example.com"
                            value="<%= formData.participantemail || '' %>"
                        />
                        <div class="field-hint">Must match the email used in registrations and surveys.</div>
                    </div>

                    <div class="form-group">
                        <label for="participantfirstname">
                            First name
                            <span class="required-indicator">*</span>
                        </label>
                        <input
                            type="text"
                            id="participantfirstname"
                            name="participantfirstname"
                            required
                            placeholder="Ella"
                            value="<%= formData.participantfirstname || '' %>"
                        />
                    </div>

                    <div class="form-group">
                        <label for="participantlastname">
                            Last name
                            <span class="required-indicator">*</span>
                        </label>
                        <input
                            type="text"
                            id="participantlastname"
                            name="participantlastname"
                            required
                            placeholder="Rivera"
                            value="<%= formData.participantlastname || '' %>"
                        />
                    </div>

                    <div class="form-group">
                        <label for="participantdob">Date of birth</label>
                        <input
                            type="date"
                            id="participantdob"
                            name="participantdob"
                            value="<%= formData.participantdob || '' %>"
                        />
                    </div>

                    <div class="form-group">
                        <label for="participantphone">Phone</label>
                        <input
                            type="text"
                            id="participantphone"
                            name="participantphone"
                            placeholder="(555) 555 5555"
                            value="<%= formData.participantphone || '' %>"
                        />
                    </div>

                    <div class="form-group">
                        <label for="participantcity">City</label>
                        <input
                            type="text"
                            id="participantcity"
                            name="participantcity"
                            placeholder="Provo"
                            value="<%= formData.participantcity || '' %>"
                        />
                    </div>

                    <div class="form-group">
                        <label for="participantstate">State</label>
                        <input
                            type="text"
                            id="participantstate"
                            name="participantstate"
                            placeholder="UT"
                            value="<%= formData.participantstate || '' %>"
                        />
                    </div>

                    <div class="form-group">
                        <label for="participantzip">ZIP code</label>
                        <input
                            type="text"
                            id="participantzip"
                            name="participantzip"
                            placeholder="84602"
                            value="<%= formData.participantzip || '' %>"
                        />
                    </div>

                    <div class="form-group">
                        <label for="participantschooloremployer">School or employer</label>
                        <input
                            type="text"
                            id="participantschooloremployer"
                            name="participantschooloremployer"
                            placeholder="BYU, UVU, local high school"
                            value="<%= formData.participantschooloremployer || '' %>"
                        />
                    </div>

                    <div class="form-group">
                        <label for="participantfieldofinterest">Field of interest</label>
                        <select id="participantfieldofinterest" name="participantfieldofinterest">
                            <option value="">Select field of interest</option>
                            <option value="stem"  <%= fd.participantfieldofinterest === "stem"  ? "selected" : "" %>>STEM</option>
                            <option value="arts"  <%= fd.participantfieldofinterest === "arts"  ? "selected" : "" %>>Arts</option>
                            <option value="both"  <%= fd.participantfieldofinterest === "both"  ? "selected" : "" %>>Both</option>
                        </select>
                        <div class="field-hint">
                            Used for reporting on STEAM focus.
                        </div>
                    </div>
                </div>

                <!-- User account section -->
                <div class="form-section">
                    <div class="form-section-title">User account</div>
                    <div class="form-section-helper">
                        These fields control how the user signs in and what they can access.
                    </div>

                    <div class="form-group">
                        <label for="username">Username</label>
                        <input
                            type="text"
                            id="username"
                            name="username"
                            placeholder="er-admin"
                            value="<%= formData.username || '' %>"
                        />
                    </div>

                    <div class="form-group">
                        <label for="role">Role</label>
                        <select id="role" name="role">
                            <option value="">Select role</option>
                            <option value="admin" <%= formData.role === "admin" ? "selected" : "" %>>Admin</option>
                            <option value="Viewer" <%= formData.role === "Viewer" ? "selected" : "" %>>Viewer</option>
                            <option value="User" <%= formData.role === "User" ? "selected" : "" %>>User</option>
                        </select>
                    </div>

                    <div class="form-group">
                        <label for="password">Password</label>
                        <input
                            type="password"
                            id="password"
                            name="password"
                            placeholder="Set a secure password"
                        />
                    </div>

                    <div class="form-group">
                        <label for="passwordConfirm">Confirm password</label>
                        <input
                            type="password"
                            id="passwordConfirm"
                            name="passwordConfirm"
                            placeholder="Re-enter password"
                        />
                    </div>
                </div>

                <hr>

                <div class="text-center-wrapper">
                    <button type="submit" class="btn-action btn-save-milestone">Create User</button>
                    <a href="/admin/users" class="btn-action btn-back">Cancel</a>
                </div>
            </form>
        </div>
    </div>

    <%- include('../common/footer.ejs') %>
</body>
</html>
