<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>View Donations | Ella Rises</title>

  <!-- Google Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=DM+Serif+Display:wght@400&family=Montserrat:wght@300;400;500;600&display=swap" rel="stylesheet">

  <style>
    :root {
      --er-blue:     #99B7C6;
      --er-lavender: #978EC4;
      --er-cream:    #F9F5EA;
      --er-blush:    #FFD8D1;
      --er-pink:     #F9AFB1;
      --er-sage:     #9AB59D;
      --er-charcoal: #3A3F3B;
      --er-peach:    #F4B89D;
    }

    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      font-family: 'Montserrat', sans-serif;
      background: var(--er-cream);
      color: var(--er-charcoal);
    }

    .page-wrapper {
      min-height: 100vh;
      padding: 24px;
      max-width: 1200px;
      margin: 24px auto 40px;
      display: flex;
      flex-direction: column;
      gap: 24px;
    }

    header {
      display: flex;
      flex-wrap: wrap;
      justify-content: space-between;
      align-items: center;
      gap: 8px;
    }

    .header-actions {
      display: flex;
      align-items: center;
      gap: 8px;
    }

    .header-actions .btn-primary {
      font-size: 0.9rem;
      padding: 9px 18px;
    }

    h1 {
      font-family: 'DM Serif Display', serif;
      font-size: 2.3rem;
      margin: 0;
      color: var(--er-lavender);
    }

    .subtitle {
      margin: 4px 0 0;
      font-size: 0.98rem;
      color: #555;
    }

    .card {
      background: linear-gradient(135deg, #ffffff, #fffaf5);
      border-radius: 18px;
      border: 1px solid #f0e2d9;
      box-shadow: 0 10px 24px rgba(0,0,0,0.04);
      padding: 20px 22px 24px;
    }

    /* Filters */

    .filters {
      display: flex;
      flex-wrap: wrap;
      gap: 16px;
      margin-bottom: 18px;
      align-items: flex-end;
      padding-bottom: 16px;
      border-bottom: 1px solid #f3ece6;
    }

    .filter-group {
      display: flex;
      flex-direction: column;
      gap: 4px;
      min-width: 180px;
      flex: 1 1 180px;
    }

    .filter-group label {
      font-size: 0.82rem;
      font-weight: 600;
      color: #666;
      text-transform: uppercase;
      letter-spacing: 0.04em;
    }

    .filter-group input,
    .filter-group select {
      padding: 8px 11px;
      border-radius: 9px;
      border: 1px solid #d6d6d6;
      font-size: 0.95rem;
      font-family: inherit;
      background-color: #fff;
      outline: none;
      transition: border-color 0.15s ease, box-shadow 0.15s ease;
    }

    .filter-group input:focus,
    .filter-group select:focus {
      border-color: var(--er-lavender);
      box-shadow: 0 0 0 2px rgba(151,142,196,0.18);
    }

    .filter-actions {
      display: flex;
      gap: 10px;
      flex-wrap: wrap;
      justify-content: flex-end;
      flex: 1 1 160px;
    }

    .btn {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      padding: 9px 16px;
      border-radius: 999px;
      border: none;
      cursor: pointer;
      font-size: 0.9rem;
      font-weight: 600;
      font-family: inherit;
      text-decoration: none;
      text-align: center;
      line-height: 1.1;
      transition: background-color 0.15s ease, color 0.15s ease,
                  box-shadow 0.15s ease, transform 0.05s ease;
    }

    .btn-sm {
      padding: 6px 12px;
      font-size: 0.82rem;
    }

    .btn-primary {
      background: var(--er-lavender);
      color: #fff;
      box-shadow: 0 6px 14px rgba(151,142,196,0.35);
    }

    .btn-primary:hover {
      background: #7c73c1;
      transform: translateY(-1px);
      box-shadow: 0 8px 18px rgba(151,142,196,0.45);
    }

    .btn-secondary {
      background: #fff;
      color: var(--er-charcoal);
      border: 1px solid #d0d0d0;
    }

    .btn-secondary:hover {
      border-color: var(--er-lavender);
      color: var(--er-lavender);
      background: #faf7ff;
    }

    .btn-danger {
      background: #f46a6a;
      color: #fff;
      border: 1px solid #f46a6a;
      box-shadow: 0 4px 10px rgba(244,106,106,0.4);
    }

    .btn-danger:hover {
      background: #dd5555;
      border-color: #dd5555;
      transform: translateY(-1px);
      box-shadow: 0 6px 14px rgba(221,85,85,0.45);
    }

    /* Table */

    .table-wrapper {
      overflow-x: auto;
      border-radius: 14px;
      border: 1px solid #eee;
      background: #fff;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      font-size: 0.9rem;
      min-width: 900px;
    }

    thead {
      background: linear-gradient(90deg, var(--er-blue), var(--er-lavender));
      color: #fff;
    }

    th,
    td {
      padding: 10px 12px;
      text-align: left;
      border-bottom: 1px solid #f3f3f3;
      white-space: nowrap;
    }

    th {
      font-weight: 600;
      font-size: 0.8rem;
      text-transform: uppercase;
      letter-spacing: 0.06em;
    }

    tbody tr:nth-child(even) {
      background: #fafafa;
    }

    tbody tr:hover {
      background: #f2f7fb;
    }

    .text-right {
      text-align: right;
    }

    td:nth-child(5) {
      font-variant-numeric: tabular-nums;
    }

    td:nth-child(6) {
      font-size: 0.85rem;
      color: #666;
      max-width: 260px;
      white-space: normal;
    }

    td:last-child {
      text-align: right;
    }

    td:last-child .btn {
      margin-left: 4px;
    }

    .tag {
      display: inline-block;
      padding: 3px 9px;
      border-radius: 999px;
      font-size: 0.75rem;
      font-weight: 600;
    }

    .tag-anon {
      background: var(--er-blush);
      color: #7a3a2f;
    }

    .tag-visible {
      background: #e5f5ec;
      color: #2f6f4a;
    }

    /* Pagination */

    .pagination {
      margin-top: 18px;
      display: flex;
      justify-content: center;
      align-items: center;
      gap: 6px;
      flex-wrap: wrap;
      font-size: 0.9rem;
    }

    .page-link,
    .page-link-disabled {
      padding: 6px 11px;
      border-radius: 999px;
      border: 1px solid #ddd;
      text-decoration: none;
      color: var(--er-charcoal);
      background: #fff;
      transition: background-color 0.15s ease,
                  border-color 0.15s ease,
                  color 0.15s ease;
    }

    .page-link:hover {
      border-color: var(--er-lavender);
      color: var(--er-lavender);
      background: #faf7ff;
    }

    .page-current {
      background: var(--er-lavender);
      color: #fff;
      border-color: var(--er-lavender);
      font-weight: 600;
    }

    .page-link-disabled {
      opacity: 0.4;
      pointer-events: none;
      cursor: default;
    }

    @media (max-width: 768px) {
      .page-wrapper {
        padding: 16px;
        margin-top: 12px;
      }

      h1 {
        font-size: 1.9rem;
      }

      .filters {
        align-items: stretch;
      }

      .filter-actions {
        justify-content: flex-start;
      }

      td:nth-child(6) {
        max-width: 180px;
      }
    }
  </style>
</head>

<body>
  <%- include('../common/navbar.ejs') %>

  <div class="page-wrapper">
    <header>
      <div>
        <h1>Donations</h1>
        <p class="subtitle">Admin view of all recorded donations for Ella Rises.</p>
      </div>

      <div class="header-actions">
        <a href="/donations/new" class="btn btn-primary">
          + Add donation
        </a>
      </div>
    </header>

    <div class="card">
      <!-- Filters -->
      <form class="filters" method="GET" action="/donations/view">
        <div class="filter-group">
          <label for="participantSearch">Participant name or email</label>
          <input
            type="text"
            id="participantSearch"
            name="participantSearch"
            placeholder="Search participant"
            value="<%= typeof participantSearch !== 'undefined' ? participantSearch : '' %>"
          />
        </div>

        <div class="filter-group">
          <label for="eventSearch">Event name</label>
          <input
            type="text"
            id="eventSearch"
            name="eventSearch"
            placeholder="Search event"
            value="<%= typeof eventSearch !== 'undefined' ? eventSearch : '' %>"
          />
        </div>

        <div class="filter-group">
          <label for="minAmount">Min amount $</label>
          <input
            type="number"
            step="0.01"
            id="minAmount"
            name="minAmount"
            placeholder="0"
            value="<%= typeof minAmount !== 'undefined' ? minAmount : '' %>"
          />
        </div>

        <div class="filter-group">
          <label for="maxAmount">Max amount $</label>
          <input
            type="number"
            step="0.01"
            id="maxAmount"
            name="maxAmount"
            placeholder="Any"
            value="<%= typeof maxAmount !== 'undefined' ? maxAmount : '' %>"
          />
        </div>

        <div class="filter-actions">
          <button type="submit" class="btn btn-primary">Apply filters</button>
          <a href="/donations/view" class="btn btn-secondary">Clear</a>
        </div>
      </form>

      <!-- Table -->
      <div class="table-wrapper">
        <table>
          <thead>
            <tr>
              <th>ID</th>
              <th>Participant</th>
              <th>Email</th>
              <th class="text-right">Amount</th>
              <th>Date</th>
              <th>Anonymous</th>
              <th>Actions</th>
            </tr>
          </thead>

          <tbody>
            <% if (donations && donations.length > 0) { %>
              <% donations.forEach(function(donation) { %>
                <tr>
                  <td><%= donation.donationid %></td>
                  <td><%= donation.participantname || 'Anonymous' %></td>
                  <td><%= donation.participantemail || 'N/A' %></td>
                  <td class="text-right">
                    $<%= Number(donation.donationamount).toFixed(2) %>
                  </td>
                  <td><%= new Date(donation.donationdate).toDateString() %></td>
                  <td>
                    <% if (donation.isanonymous) { %>
                      <span class="tag tag-anon">Anonymous</span>
                    <% } else { %>
                      <span class="tag tag-visible">Visible</span>
                    <% } %>
                  </td>
                  <td>
                    <a
                      href="/donations/<%= donation.donationid %>/edit"
                      class="btn btn-secondary btn-sm"
                    >
                      Edit
                    </a>

                    <form
                      action="/donations/<%= donation.donationid %>/delete"
                      method="post"
                      style="display:inline;"
                      onsubmit="return confirm('Are you sure you want to delete this donation? This cannot be undone.');"
                    >
                      <button type="submit" class="btn btn-danger btn-sm">
                        Delete
                      </button>
                    </form>
                  </td>
                </tr>
              <% }); %>
            <% } else { %>
              <tr>
                <td colspan="7">No donations found with the current filters.</td>
              </tr>
            <% } %>
          </tbody>
        </table>
      </div>

      <!-- Pagination -->
      <% if (pagination && pagination.totalPages > 1) { %>
        <div class="pagination">
          <%
            const hasPrev = pagination.currentPage > 1;
            const hasNext = pagination.currentPage < pagination.totalPages;

            function pageUrl(page) {
              let url = "/donations/view?page=" + page;

              if (participantSearch && participantSearch.trim() !== "") {
                url += "&participantSearch=" + encodeURIComponent(participantSearch.trim());
              }
              if (eventSearch && eventSearch.trim() !== "") {
                url += "&eventSearch=" + encodeURIComponent(eventSearch.trim());
              }
              if (minAmount && String(minAmount).trim() !== "") {
                url += "&minAmount=" + encodeURIComponent(minAmount);
              }
              if (maxAmount && String(maxAmount).trim() !== "") {
                url += "&maxAmount=" + encodeURIComponent(maxAmount);
              }

              return url;
            }
          %>

          <!-- Prev button -->
          <% if (hasPrev) { %>
            <a class="page-link" href="<%= pageUrl(pagination.currentPage - 1) %>">
              Prev
            </a>
          <% } else { %>
            <span class="page-link-disabled">Prev</span>
          <% } %>

          <!-- First page plus leading ellipsis -->
          <% if (pagination.windowStart > 1) { %>
            <a
              class="page-link <%= pagination.currentPage === 1 ? 'page-current' : '' %>"
              href="<%= pageUrl(1) %>"
            >
              1
            </a>

            <% if (pagination.windowStart > 2) { %>
              <a
                class="page-link"
                href="<%= pageUrl(Math.max(1, pagination.windowStart - pagination.windowSize)) %>"
              >
                ...
              </a>
            <% } %>
          <% } %>

          <!-- Window of pages -->
          <% for (let p = pagination.windowStart; p <= pagination.windowEnd; p++) { %>
            <a
              class="page-link <%= p === pagination.currentPage ? 'page-current' : '' %>"
              href="<%= pageUrl(p) %>"
            >
              <%= p %>
            </a>
          <% } %>

          <!-- Trailing ellipsis plus last page -->
          <% if (pagination.windowEnd < pagination.totalPages) { %>
            <% if (pagination.windowEnd < pagination.totalPages - 1) { %>
              <a
                class="page-link"
                href="<%= pageUrl(Math.min(pagination.totalPages, pagination.windowEnd + pagination.windowSize)) %>"
              >
                ...
              </a>
            <% } %>

            <a
              class="page-link <%= pagination.currentPage === pagination.totalPages ? 'page-current' : '' %>"
              href="<%= pageUrl(pagination.totalPages) %>"
            >
              <%= pagination.totalPages %>
            </a>
          <% } %>

          <!-- Next button -->
          <% if (hasNext) { %>
            <a class="page-link" href="<%= pageUrl(pagination.currentPage + 1) %>">
              Next
            </a>
          <% } else { %>
            <span class="page-link-disabled">Next</span>
          <% } %>
        </div>
      <% } %>
    </div>
  </div>
</body>
</html>
