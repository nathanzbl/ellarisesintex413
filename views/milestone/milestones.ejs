<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Milestones | Ella Rises</title>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=DM+Serif+Display:wght@400&family=Montserrat:wght@300;400;500;600&display=swap" rel="stylesheet">

    <style>
        :root {
            --er-blue:     #99B7C6;
            --er-lavender: #978EC4;
            --er-cream:    #F9F5EA;
            --er-blush:    #FFD8D1;
            --er-pink:     #F9AFB1;
            --er-sage:     #9AB59D;
            --er-charcoal: #3A3F3B;
            --er-peach:    #F4B092;
            --er-rose:     #CE325B;
        }

        * { box-sizing: border-box; }

        body {
            margin: 0;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 0;
            font-family: "Montserrat", system-ui, sans-serif;
            background: var(--er-cream);
            color: var(--er-charcoal);
        }

        .main-content {
            flex: 1;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 2rem 1rem;
            width: 100%;
        }

        .data-card {
            max-width: 1200px;
            width: 100%;
            padding: 2.5rem 2.25rem;
            border-radius: 1.5rem;
            background-color: #ffffff;
            box-shadow: 0 14px 40px rgba(0, 0, 0, 0.08);
            border-top: 6px solid var(--er-pink);
        }

        .milestone-title {
            font-family: "DM Serif Display", serif;
            font-size: 2rem;
            text-align: center;
            letter-spacing: 0.06em;
            margin: 0 0 0.25rem;
        }

        .milestone-subtitle {
            font-size: 0.95rem;
            text-align: center;
            color: rgba(58, 63, 59, 0.7);
            margin-bottom: 2rem;
        }

        .controls-bar {
            display: flex;
            flex-wrap: wrap;
            align-items: flex-end;
            justify-content: space-between;
            margin-bottom: 1.5rem;
            padding-bottom: 1.5rem;
            border-bottom: 1px solid var(--er-blush);
        }

        .search-form {
            display: flex;
            flex-grow: 1;
            flex-wrap: wrap;
            gap: 1rem;
            align-items: flex-end;
            max-width: 100%;
        }

        .add-button-wrapper {
            text-align: center;
            flex-shrink: 0;
            padding-bottom: 0.7rem;
        }

        .search-group {
            display: flex;
            flex-direction: column;
            flex-grow: 1;
            min-width: 150px;
        }

        .search-label {
            font-size: 0.75rem;
            font-weight: 500;
            margin-bottom: 0.5rem;
            color: var(--er-charcoal);
            text-transform: uppercase;
        }

        .search-input {
            padding: 0.7rem 1rem;
            border: 2px solid var(--er-blush);
            border-radius: 0.75rem;
            font-family: inherit;
            font-size: 0.9rem;
            outline: none;
            transition: border-color 0.2s ease;
        }

        .search-input:focus {
            border-color: var(--er-pink);
        }

        .btn-search,
        .btn-clear {
            padding: 0.7rem 1rem;
            border-radius: 999px;
            border: none;
            font-weight: 600;
            font-size: 0.85rem;
            text-transform: uppercase;
            cursor: pointer;
            transition: all 0.18s ease;
            width: 100%;
        }

        .btn-search {
            background: var(--er-sage);
            color: white;
            box-shadow: 0 5px 12px rgba(154, 181, 157, 0.4);
        }

        .btn-search:hover {
            background: rgba(154, 181, 157, 0.8);
        }

        .btn-clear {
            background: var(--er-blush);
            color: var(--er-charcoal);
            text-decoration: none;
            display: inline-block;
            text-align: center;
            margin-top: 0.5rem;
        }

        .btn-clear:hover {
            background: var(--er-pink);
            color: white;
        }

        .controls-actions-group {
            display: flex;
            gap: 0.75rem;
            align-items: flex-start;
        }

        .search-controls {
            display: flex;
            flex-direction: column;
            flex: 1;
        }

        .table-custom {
            width: 100%;
            border-collapse: collapse;
            margin-top: 1.5rem;
        }

        .table-custom thead tr {
            background-color: var(--er-charcoal);
            color: var(--er-cream);
            font-size: 0.8rem;
            letter-spacing: 0.08em;
            text-transform: uppercase;
        }

        .table-custom th,
        .table-custom td {
            padding: 1rem 1.25rem;
            text-align: left;
            border-bottom: 1px solid var(--er-blush);
        }

        .table-responsive-wrapper {
            overflow-x: auto;
            width: 100%;
            -webkit-overflow-scrolling: touch;
        }

        .table-custom tbody tr:nth-child(even) {
            background-color: var(--er-cream);
        }

        .btn-action {
            padding: 0.7rem 1rem;
            border-radius: 999px;
            border: none;
            font-weight: 600;
            font-size: 0.85rem;
            letter-spacing: 0.08em;
            text-transform: uppercase;
            cursor: pointer;
            box-shadow: 0 10px 24px rgba(206, 50, 91, 0.25);
            transition: all 0.18s ease;
            text-decoration: none;
            display: inline-block;
        }

        .btn-add-milestone {
            background: var(--er-pink);
            color: #fff;
        }

        .btn-add-milestone:hover {
            background: var(--er-rose);
            transform: translateY(-1px);
        }

        .btn-small {
            padding: 0.75rem 1rem;
            border-radius: 999px;
            border: none;
            font-weight: 600;
            font-size: 0.65rem;
            letter-spacing: 0.08em;
            text-transform: uppercase;
            cursor: pointer;
            box-shadow: 0 5px 12px rgba(32, 131, 47, 0.25);
            transition: all 0.18s ease;
            text-decoration: none;
            display: inline-block;
            text-align: center;
        }

        .btn-edit-milestone {
            background: var(--er-sage);
            color: #fff;
            box-shadow: none;
        }

        .btn-edit-milestone:hover {
            background: var(--er-blue);
            transform: translateY(-1px);
        }

        .btn-delete-milestone {
            background: var(--er-rose);
            color: #fff;
            box-shadow: none;
        }

        .btn-delete-milestone:hover {
            background: var(--er-charcoal);
            transform: translateY(-1px);
        }

        .message-alert {
            text-align: center;
            color: var(--er-rose);
            background: rgba(206, 50, 91, 0.07);
            padding: 0.8rem 1.2rem;
            border-radius: 0.75rem;
            margin-bottom: 1.5rem;
        }

        .message-warning {
            color: var(--er-charcoal);
            background: var(--er-blush);
            border: 1px solid var(--er-pink);
        }

        /* new pagination styles */
        .pagination {
            margin-top: 24px;
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 6px;
            flex-wrap: wrap;
            font-size: 0.9rem;
        }

        .page-link,
        .page-link-disabled {
            padding: 6px 11px;
            border-radius: 999px;
            border: 1px solid #ddd;
            text-decoration: none;
            color: var(--er-charcoal);
            background: #fff;
            transition: background-color 0.15s ease,
                        border-color 0.15s ease,
                        color 0.15s ease;
        }

        .page-link:hover {
            border-color: var(--er-lavender);
            color: var(--er-lavender);
            background: #faf7ff;
        }

        .page-current {
            background: var(--er-lavender);
            color: #fff;
            border-color: var(--er-lavender);
            font-weight: 600;
        }

        .page-link-disabled {
            opacity: 0.4;
            pointer-events: none;
            cursor: default;
        }
    </style>

    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
</head>
<body>
    <%- include('../common/navbar.ejs') %>

    <div class="main-content">
        <div class="data-card">
            <h1 class="milestone-title">Participant Milestones</h1>
            <p class="milestone-subtitle">A view of all milestones recorded in the system.</p>

            <div class="add-button-wrapper">
                <a href="/addmilestone" class="btn-action btn-add-milestone">Add Milestone</a>
            </div>

            <div class="controls-bar">
                <form method="GET" action="/milestones" class="search-form">
                    <div class="search-group" style="flex-basis: 25%;">
                        <label for="search_name" class="search-label">Participant Name</label>
                        <input
                            type="text"
                            name="search_name"
                            id="search_name"
                            class="search-input"
                            placeholder="e.g., Jane Doe"
                            value="<%= typeof search_name !== 'undefined' ? search_name : '' %>"
                        >
                    </div>

                    <div class="search-group" style="flex-basis: 25%;">
                        <label for="search_milestone" class="search-label">Milestone Title</label>
                        <input
                            type="text"
                            name="search_milestone"
                            id="search_milestone"
                            class="search-input"
                            placeholder="e.g., Diploma"
                            value="<%= typeof search_milestone !== 'undefined' ? search_milestone : '' %>"
                        >
                    </div>

                    <div class="search-group" style="flex-basis: 15%;">
                        <label for="date" class="search-label">To Date</label>
                        <input
                            type="date"
                            name="date"
                            id="date"
                            class="search-input"
                            value="<%= typeof date !== 'undefined' ? date : '' %>"
                        >
                    </div>

                    <div class="search-group controls-actions-group" style="flex-basis: 20%; flex-grow: 0;">
                        <div class="search-controls">
                            <button type="submit" class="btn-search">Search</button>

                            <%
                              const isSearching =
                                (typeof search_name !== 'undefined' && search_name) ||
                                (typeof search_milestone !== 'undefined' && search_milestone) ||
                                (typeof date !== 'undefined' && date);
                            %>

                            <% if (isSearching) { %>
                                <a href="/milestones" class="btn-clear">Clear Filters</a>
                            <% } %>
                        </div>
                    </div>
                </form>
            </div>

            <% if (error_message) { %>
                <div class="message-alert">
                    <%= error_message %>
                </div>
            <% } %>

            <% if (!milestone || milestone.length === 0) { %>
                <div class="message-alert message-warning">
                    No milestones found <%= isSearching ? 'matching your criteria.' : 'in the database.' %>
                </div>
            <% } else { %>
                <div class="table-responsive-wrapper">
                    <table class="table-custom">
                        <thead>
                            <tr>
                                <th>Participant ID</th>
                                <th>Participant Name</th>
                                <th>Participant Email</th>
                                <th>Milestone Title</th>
                                <th>Date Achieved</th>
                                <th>Actions</th>

                            </tr>
                        </thead>
                        <tbody>
                            <% for (let i = 0; i < milestone.length; i++) { %>
                                <tr>
                                    <td><%= milestone[i].participantid %></td>
                                    <td>
                                        <%= milestone[i].participantfirstname %>
                                        <%= milestone[i].participantlastname %>
                                    </td>
                                    <td><%= milestone[i].participantemail %></td>
                                    <td><%= milestone[i].milestonetitle %></td>
                                    <td>
                                        <%
                                          const dateOptions = { year: 'numeric', month: 'long', day: 'numeric' };
                                          const dateValue = new Date(milestone[i].milestonedate);
                                        %>
                                        <%= dateValue.toLocaleDateString('en-US', dateOptions) %>
                                    </td>
                                    <td>
                                        <form action="/editMilestone/<%= milestone[i].milestoneid %>" method="get">
                                            <input type="submit" name="Edit" value="Edit" class="btn-small btn-edit-milestone" />
                                        </form>
                                        <form
                                            action="/deleteMilestone/<%= milestone[i].milestoneid %>"
                                            method="post"
                                            onsubmit="return confirmDeletion(event);"
                                        >
                                            <input type="submit" name="Delete" value="Delete" class="btn-small btn-delete-milestone" />
                                        </form>
                                    </td>

                                        

                                </tr>
                            <% } %>
                        </tbody>
                    </table>
                </div>
            <% } %>

            <% if (pagination && pagination.totalPages > 1) { %>
                <div class="pagination">
                    <%
                      const hasPrev = pagination.currentPage > 1;
                      const hasNext = pagination.currentPage < pagination.totalPages;

                      function pageUrl(page) {
                        let url = "/milestones?page=" + page;

                        if (search_name && search_name.trim() !== "") {
                          url += "&search_name=" + encodeURIComponent(search_name.trim());
                        }
                        if (search_milestone && search_milestone.trim() !== "") {
                          url += "&search_milestone=" + encodeURIComponent(search_milestone.trim());
                        }
                        if (date && date.trim() !== "") {
                          url += "&date=" + encodeURIComponent(date.trim());
                        }

                        return url;
                      }
                    %>

                    <% if (hasPrev) { %>
                        <a class="page-link" href="<%= pageUrl(pagination.currentPage - 1) %>">
                            Prev
                        </a>
                    <% } else { %>
                        <span class="page-link-disabled">Prev</span>
                    <% } %>

                    <% if (pagination.windowStart > 1) { %>
                        <a
                          class="page-link <%= pagination.currentPage === 1 ? 'page-current' : '' %>"
                          href="<%= pageUrl(1) %>"
                        >
                          1
                        </a>

                        <% if (pagination.windowStart > 2) { %>
                          <a
                            class="page-link"
                            href="<%= pageUrl(Math.max(1, pagination.windowStart - pagination.windowSize)) %>"
                          >
                            ...
                          </a>
                        <% } %>
                    <% } %>

                    <% for (let p = pagination.windowStart; p <= pagination.windowEnd; p++) { %>
                        <a
                          class="page-link <%= p === pagination.currentPage ? 'page-current' : '' %>"
                          href="<%= pageUrl(p) %>"
                        >
                          <%= p %>
                        </a>
                    <% } %>

                    <% if (pagination.windowEnd < pagination.totalPages) { %>
                        <% if (pagination.windowEnd < pagination.totalPages - 1) { %>
                          <a
                            class="page-link"
                            href="<%= pageUrl(Math.min(pagination.totalPages, pagination.windowEnd + pagination.windowSize)) %>"
                          >
                            ...
                          </a>
                        <% } %>

                        <a
                          class="page-link <%= pagination.currentPage === pagination.totalPages ? 'page-current' : '' %>"
                          href="<%= pageUrl(pagination.totalPages) %>"
                        >
                          <%= pagination.totalPages %>
                        </a>
                    <% } %>

                    <% if (hasNext) { %>
                        <a class="page-link" href="<%= pageUrl(pagination.currentPage + 1) %>">
                            Next
                        </a>
                    <% } else { %>
                        <span class="page-link-disabled">Next</span>
                    <% } %>
                </div>
            <% } %>

        </div>
    </div>

    <%- include('../common/footer.ejs') %>

    <script>
        function confirmDeletion(event) {
            event.preventDefault();
            const form = event.currentTarget;

            Swal.fire({
                title: "Are you sure?",
                text: "You will not be able to revert this.",
                icon: "warning",
                showCancelButton: true,
                confirmButtonColor: "#CE325B",
                cancelButtonColor: "#9AB59D",
                confirmButtonText: "Yes, delete it."
            }).then((result) => {
                if (result.isConfirmed) {
                    form.submit();
                }
            });

            return false;
        }
    </script>
</body>
</html>
