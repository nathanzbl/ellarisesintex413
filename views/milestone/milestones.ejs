<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Milestones | Ella Rises</title>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=DM+Serif+Display:wght@400&family=Montserrat:wght@300;400;500;600&display=swap" rel="stylesheet">

    <style>
        :root {
            --er-blue:     #99B7C6;
            --er-lavender: #978EC4;
            --er-cream:    #F9F5EA;
            --er-blush:    #FFD8D1;
            --er-pink:     #F9AFB1;
            --er-sage:     #9AB59D;
            --er-charcoal: #3A3F3B;
            --er-peach:    #F4B092;
            --er-rose:     #CE325B;
        }

        * { box-sizing: border-box; }

        body {
            margin: 0;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 0;
            font-family: "Montserrat", system-ui, sans-serif;
            background: var(--er-cream);
            color: var(--er-charcoal);
        }

        .main-content {
            flex: 1;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 2rem 1rem;
            width: 100%;
        }

        .data-card {
            max-width: 1200px; /* Wider for table content */
            width: 100%;
            padding: 2.5rem 2.25rem;
            border-radius: 1.5rem;
            background-color: #ffffff;
            box-shadow: 0 14px 40px rgba(0, 0, 0, 0.08);
            border-top: 6px solid var(--er-pink);
        }

        .milestone-title {
            font-family: "DM Serif Display", serif;
            font-size: 2rem;
            text-align: center;
            letter-spacing: 0.06em;
            margin: 0 0 0.25rem;
        }

        .milestone-subtitle {
            font-size: 0.95rem;
            text-align: center;
            color: rgba(58, 63, 59, 0.7);
            margin-bottom: 2rem;
        }
        
        /* --- NEW CONTROL BAR STYLING --- */
        .controls-bar {
            display: flex;
            flex-wrap: wrap;
            align-items: flex-end; /* Aligns items to the bottom */
            justify-content: space-between;
            margin-bottom: 1.5rem;
            padding-bottom: 1.5rem;
            border-bottom: 1px solid var(--er-blush);
        }
        
        .search-form {
            display: flex;
            flex-grow: 1;
            flex-wrap: wrap; /* Allows wrapping on mobile */
            gap: 1rem;
            align-items: flex-end;
            /* Ensures the search inputs take available space */
            max-width: calc(100% - 150px); 
        }

        .add-button-wrapper {
            /* Keeps the button aligned to the right */
            flex-shrink: 0; 
            margin-left: 1rem; 
            padding-bottom: 0.7rem; /* Vertically align with Search button */
        }
        /* --- END NEW CONTROL BAR STYLING --- */


        /* --- SEARCH AND FILTER STYLES --- */
        .search-group {
            display: flex;
            flex-direction: column;
            flex-grow: 1;
            min-width: 150px; /* Ensures fields are usable on small screens */
        }
        
        .search-label {
            font-size: 0.75rem;
            font-weight: 500;
            margin-bottom: 0.5rem;
            color: var(--er-charcoal);
            text-transform: uppercase;
        }
        
        .search-input {
            padding: 0.7rem 1rem;
            border: 2px solid var(--er-blush);
            border-radius: 0.75rem;
            font-family: inherit;
            font-size: 0.9rem;
            outline: none;
            transition: border-color 0.2s ease;
        }
        
        .search-input:focus {
            border-color: var(--er-pink);
        }

        .btn-search, .btn-clear {
            padding: 0.7rem 1rem;
            border-radius: 999px;
            border: none;
            font-weight: 600;
            font-size: 0.85rem;
            text-transform: uppercase;
            cursor: pointer;
            transition: all 0.18s ease;
            width: 100%;
        }

        .btn-search {
            background: var(--er-sage);
            color: white;
            box-shadow: 0 5px 12px rgba(154, 181, 157, 0.4);
        }
        .btn-search:hover {
            background: rgba(154, 181, 157, 0.8);
        }
        
        .btn-clear {
            background: var(--er-blush);
            color: var(--er-charcoal);
            text-decoration: none;
            display: inline-block;
            text-align: center;
            margin-top: 0.5rem;
        }
        .btn-clear:hover {
            background: var(--er-pink);
            color: white;
        }
        /* --- END SEARCH STYLES --- */


        .table-custom {
            width: 100%;
            border-collapse: collapse;
            margin-top: 1.5rem;
        }

        .table-custom thead tr {
            background-color: var(--er-charcoal);
            color: var(--er-cream);
            font-size: 0.8rem;
            letter-spacing: 0.08em;
            text-transform: uppercase;
        }
        
        .table-custom th,
        .table-custom td {
            padding: 1rem 1.25rem;
            text-align: left;
            border-bottom: 1px solid var(--er-blush);
        }

        .table-custom tbody tr:nth-child(even) {
            background-color: var(--er-cream);
        }

        .btn-action {
            padding: 0.85rem 1rem;
            border-radius: 999px;
            border: none;
            font-weight: 600;
            font-size: 0.95rem;
            letter-spacing: 0.08em;
            text-transform: uppercase;
            cursor: pointer;
            box-shadow: 0 10px 24px rgba(206, 50, 91, 0.25);
            transition: all 0.18s ease;
            text-decoration: none;
            display: inline-block;
            text-align: center;
        }

        .btn-add-milestone {
            background: var(--er-pink);
            color: #fff;
        }
        
        .btn-add-milestone:hover {
            background: var(--er-rose);
            transform: translateY(-1px);
        }

        .btn-small {
            padding: 0.75rem 1rem;
            border-radius: 999px;
            border: none;
            font-weight: 600;
            font-size: 0.65rem;
            letter-spacing: 0.08em;
            text-transform: uppercase;
            cursor: pointer;
            box-shadow: 0 5px 12px rgba(32, 131, 47, 0.25);
            transition: all 0.18s ease;
            text-decoration: none;
            display: inline-block;
            text-align: center;
        }

        .btn-edit-milestone {
            background: var(--er-sage);
            color: #fff;
            box-shadow: none; /* Removed shadow on small buttons */
        }
        
        .btn-edit-milestone:hover {
            background: var(--er-blue);
            transform: translateY(-1px);
        }
        
        .btn-delete-milestone {
            background: var(--er-rose);
            color: #fff;
            box-shadow: none; /* Removed shadow on small buttons */
        }
        
        .btn-delete-milestone:hover {
            background: var(--er-charcoal);
            transform: translateY(-1px);
        }


        .btn-logout {
            background: var(--er-charcoal);
            color: #fff;
        }
        
        .btn-logout:hover {
            background: rgba(58, 63, 59, 0.8);
            transform: translateY(-1px);
        }

        .message-alert {
            text-align: center;
            color: var(--er-rose);
            background: rgba(206, 50, 91, 0.07);
            padding: 0.8rem 1.2rem;
            border-radius: 0.75rem;
            margin-bottom: 1.5rem;
        }

        .message-warning {
            color: var(--er-charcoal);
            background: var(--er-blush);
            border: 1px solid var(--er-pink);
        }
        
        .text-center-wrapper {
             text-align: center;
             margin-top: 2rem;
        }

        .pagination-container {
            display: flex;
            justify-content: center;
            margin-top: 2rem;
        }

        .custom-pagination {
            display: flex;
            list-style: none;
            padding: 0;
            border-radius: 0.75rem;
            overflow: hidden; 
        }
        
        .page-link {
            text-decoration: none;
            color: var(--er-charcoal);
            padding: 0.75rem 1rem;
            border: 1px solid var(--er-blush);
            transition: background-color 0.2s ease;
            font-size: 0.9rem;
            font-weight: 500;
        }

        .page-item:not(:first-child) .page-link {
            border-left: none;
        }

        .page-item.active .page-link {
            background-color: var(--er-pink);
            border-color: var(--er-pink);
            color: white;
            box-shadow: 0 4px 10px rgba(249, 175, 177, 0.4);
            z-index: 1; 
        }

        .page-link:hover {
            background-color: var(--er-blush);
        }
        
        .page-item.disabled .page-link {
            color: rgba(58, 63, 59, 0.5);
            background-color: var(--er-cream);
            cursor: default;
            pointer-events: none;
        }
    </style>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
</head>
<body>
    <%- include('../navbar.ejs') %>

    <div class="main-content">
        <div class="data-card">
            
            
            <h1 class="milestone-title">Participant Milestones</h1>
            <p class="milestone-subtitle">A view of all milestones recorded in the system.</p>

            <!-- START TOP CONTROLS BAR: SEARCH FILTERS AND ADD BUTTON -->
            <div class="controls-bar">
                
                <!-- Search Form occupies the majority of the space -->
                <form method="GET" action="/milestones" class="search-form">
                    
                    <div class="search-group" style="flex-basis: 25%;">
                        <label for="search_name" class="search-label">Participant Name</label>
                        <input 
                            type="text" 
                            name="search_name" 
                            id="search_name" 
                            class="search-input" 
                            placeholder="e.g., Jane Doe"
                            value="<%= typeof search_name !== 'undefined' ? search_name : '' %>"
                        >
                    </div>

                    <div class="search-group" style="flex-basis: 25%;">
                        <label for="search_milestone" class="search-label">Milestone Title</label>
                        <input 
                            type="text" 
                            name="search_milestone" 
                            id="search_milestone" 
                            class="search-input" 
                            placeholder="e.g., Diploma"
                            value="<%= typeof search_milestone !== 'undefined' ? search_milestone : '' %>"
                        >
                    </div>
                    
                    <div class="search-group" style="flex-basis: 15%;">
                        <label for="date" class="search-label">To Date</label>
                        <input 
                            type="date" 
                            name="date" 
                            id="date" 
                            class="search-input"
                            value="<%= typeof date !== 'undefined' ? date : '' %>"
                        >
                    </div>

                    <div class="search-group" style="flex-basis: 15%; flex-grow: 0;">
                        <button type="submit" class="btn-search">Search</button>
                        
                        <% 
                            // Check the three unique variables: search_name, search_milestone, date
                            const isSearching = (typeof search_name !== 'undefined' && search_name) || 
                                                (typeof search_milestone !== 'undefined' && search_milestone) ||
                                                (typeof date !== 'undefined' && date);
                        %>

                        <% if (isSearching) { %>
                            <a href="/milestones" class="btn-clear">Clear Filters</a>
                        <% } %>
                    </div>
                </form>
                
                <!-- Add Milestone Button aligned to the right -->
                <div class="add-button-wrapper">
                    <a href="/addmilestone" class="btn-action btn-add-milestone">Add Milestone</a>
                </div>
            </div>
            <!-- END TOP CONTROLS BAR -->

            <% if (typeof totalPages !== 'undefined' && totalPages > 1) { %>
                <div class="pagination-container">
                    <ul class="custom-pagination">
                        
                        <!-- Previous Button -->
                        <li class="page-item <%= currentPage === 1 ? 'disabled' : '' %>">
                            <!-- Preserve search terms in pagination links -->
                            <a class="page-link" href="/milestones?page=<%= currentPage - 1 %><%= isSearching ? '&search_name=' + (search_name || '') + '&search_milestone=' + (search_milestone || '') + '&date=' + (date || '') : '' %>">Previous</a>
                        </li>

                        <!-- Page Number Links -->
                        <% for(let i = 1; i <= totalPages; i++) { %>
                            <li class="page-item <%= currentPage === i ? 'active' : '' %>">
                                <!-- Preserve search terms in pagination links -->
                                <a class="page-link" href="/milestones?page=<%= i %><%= isSearching ? '&search_name=' + (search_name || '') + '&search_milestone=' + (search_milestone || '') + '&date=' + (date || '') : '' %>"><%= i %></a>
                            </li>
                        <% } %>

                        <!-- Next Button -->
                        <li class="page-item <%= currentPage === totalPages ? 'disabled' : '' %>">
                            <!-- Preserve search terms in pagination links -->
                            <a class="page-link" href="/milestones?page=<%= currentPage + 1 %><%= isSearching ? '&search_name=' + (search_name || '') + '&search_milestone=' + (search_milestone || '') + '&date=' + (date || '') : '' %>">Next</a>
                        </li>
                    </ul>
                </div>
            <% } %>
                
            <% if (typeof error_message !== 'undefined' && error_message) { %>
                <div class="message-alert">
                    <%= error_message %>
                </div>
            <% } %>

            <% if (milestone.length === 0) { %>
                <div class="message-alert message-warning">
                    No milestones found <%= isSearching ? 'matching your criteria.' : 'in the database.' %>
                </div>
            <% } else { %>
                <table class="table-custom">
                    <thead>
                        <tr>
                            <th>Participant Name</th>
                            <th>Participant Email</th>
                            <th>Milestone Title</th>
                            <th>Date Achieved</th>
                            <th></th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        <% for (let iCount = 0; iCount < milestone.length; iCount++) { %>
                            <tr>
                                <td><%= milestone[iCount].participantfirstname %> <%= milestone[iCount].participantlastname %></td>
                                <td><%= milestone[iCount].participantemail %> </td>
                                <td><%= milestone[iCount].milestonetitle %></td>
                                <td>
                                    <% 
                                        const dateOptions = { year: 'numeric', month: 'long', day: 'numeric' };
                                        const dateValue = new Date(milestone[iCount].milestonedate);
                                    %>
                                    <%= dateValue.toLocaleDateString('en-US', dateOptions) %>
                                </td>
                                <td>
                                    <form action="/editMilestone/<%= milestone[iCount].milestoneid %>" method="get">
                                        <input type="submit" name="Edit" value="Edit" class="btn-small btn-edit-milestone"/>
                                    </form>
                                </td>
                                <td>
                                    <form 
                                        action="/deleteMilestone/<%= milestone[iCount].milestoneid %>" 
                                        method="post"
                                        onsubmit="return confirmDeletion(event);" 
                                    >
                                        <input type="submit" name="Delete" value="Delete" class="btn-small btn-delete-milestone"/>
                                    </form>
                                </td>
                            </tr>
                        <% } %>
                    </tbody>
                </table>
            <% } %>

            <% if (typeof totalPages !== 'undefined' && totalPages > 1) { %>
                <div class="pagination-container">
                    <ul class="custom-pagination">
                        
                        <!-- Previous Button -->
                        <li class="page-item <%= currentPage === 1 ? 'disabled' : '' %>">
                            <!-- Preserve search terms in pagination links -->
                            <a class="page-link" href="/milestones?page=<%= currentPage - 1 %><%= isSearching ? '&search_name=' + (search_name || '') + '&search_milestone=' + (search_milestone || '') + '&date=' + (date || '') : '' %>">Previous</a>
                        </li>

                        <!-- Page Number Links -->
                        <% for(let i = 1; i <= totalPages; i++) { %>
                            <li class="page-item <%= currentPage === i ? 'active' : '' %>">
                                <!-- Preserve search terms in pagination links -->
                                <a class="page-link" href="/milestones?page=<%= i %><%= isSearching ? '&search_name=' + (search_name || '') + '&search_milestone=' + (search_milestone || '') + '&date=' + (date || '') : '' %>"><%= i %></a>
                            </li>
                        <% } %>

                        <!-- Next Button -->
                        <li class="page-item <%= currentPage === totalPages ? 'disabled' : '' %>">
                            <!-- Preserve search terms in pagination links -->
                            <a class="page-link" href="/milestones?page=<%= currentPage + 1 %><%= isSearching ? '&search_name=' + (search_name || '') + '&search_milestone=' + (search_milestone || '') + '&date=' + (date || '') : '' %>">Next</a>
                        </li>
                    </ul>
                </div>
            <% } %>
            
        </div>
    </div>
    <%- include('../common/footer.ejs') %>
    
</body>
<script>
    function confirmDeletion(event) {
    // Prevent the form from submitting immediately
    event.preventDefault(); 

    const form = event.currentTarget; // Get the form element that triggered the submit

    Swal.fire({
        title: "Are you sure?",
        text: "You won't be able to revert this!",
        icon: "warning",
        showCancelButton: true,
        confirmButtonColor: "#CE325B", 
        cancelButtonColor: "#9AB59D",
        confirmButtonText: "Yes, delete it!"
    }).then((result) => {
        if (result.isConfirmed) {
            form.submit();
        }
    });
}
</script>
</html>