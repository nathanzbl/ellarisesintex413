<!DOCTYPE html>
<html lang="en">
<head>
    <style>
    :root {
        --er-blue:     #99B7C6;
        --er-lavender: #978EC4;
        --er-cream:    #F9F5EA;
        --er-blush:    #FFD8D1;
        --er-pink:     #F9AFB1;
        --er-sage:     #9AB59D;
        --er-charcoal: #3A3F3B;
        --er-peach:    #F4B092;
        --er-rose:     #CE325B;
    }

    * { box-sizing: border-box; }

    body {
        margin: 0;
        min-height: 100vh;
        display: flex;
        flex-direction: column;
        align-items: center;
        padding: 0;
        font-family: "Montserrat", system-ui, sans-serif;
        background: var(--er-cream);
        color: var(--er-charcoal);
    }

    .main-content {
        flex: 1;
        display: flex;
        align-items: flex-start; /* Align content to the top */
        justify-content: center;
        padding: 2rem 1rem;
        width: 100%;
    }

    .data-card {
        max-width: 1200px; 
        width: 100%;
        padding: 2.5rem 2.25rem;
        border-radius: 1.5rem;
        background-color: #ffffff;
        box-shadow: 0 14px 40px rgba(0, 0, 0, 0.08);
        border-top: 6px solid var(--er-pink);
    }

    .dashboard-title {
        font-family: "DM Serif Display", serif;
        font-size: 2.5rem;
        text-align: center;
        letter-spacing: 0.06em;
        color: var(--er-charcoal);
        margin: 0 0 0.5rem;
    }

    .dashboard-subtitle {
        font-size: 1rem;
        text-align: center;
        color: var(--er-rose);
        margin-bottom: 3rem;
        font-weight: 600;
    }

    .section-header {
        font-family: "DM Serif Display", serif;
        font-size: 1.8rem;
        color: var(--er-lavender);
        border-bottom: 2px solid var(--er-blush);
        padding-bottom: 0.5rem;
        margin-top: 2.5rem;
        margin-bottom: 1.5rem;
    }

    /* --- PROFILE GRID --- */
    .profile-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
        gap: 1.5rem;
    }

    .profile-item {
        background: var(--er-cream);
        padding: 1rem;
        border-radius: 0.75rem;
        border: 1px solid var(--er-blush);
    }

    .profile-label {
        font-size: 0.75rem;
        font-weight: 600;
        text-transform: uppercase;
        color: var(--er-charcoal);
        margin-bottom: 0.25rem;
    }

    .profile-value {
        font-size: 1rem;
        font-weight: 500;
        color: var(--er-rose);
    }

    /* --- TABLE STYLES (Milestones/Donations) --- */
    .table-responsive {
        overflow-x: auto;
    }

    .table-dashboard {
        width: 100%;
        min-width: 600px;
        border-collapse: collapse;
        margin-top: 1rem;
    }

    .table-dashboard thead tr {
        background-color: var(--er-sage);
        color: white;
        font-size: 0.8rem;
        letter-spacing: 0.05em;
        text-transform: uppercase;
    }

    .table-dashboard th,
    .table-dashboard td {
        padding: 0.8rem 1rem;
        text-align: left;
        border-bottom: 1px solid var(--er-blush);
    }

    .table-dashboard tbody tr:nth-child(even) {
        background-color: var(--er-blush);
    }

    .text-center {
        text-align: center;
    }
</style>

    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>User Dashboard | Ella Rises</title>
</head>
<body>
    <%- include('common/navbar.ejs') %>

    <div class="main-content">
        <div class="data-card">
            <h1 class="dashboard-title">Welcome, <%= users.username %>!</h1>
            <p class="dashboard-subtitle">Your Ella Rises Program Summary</p>

            
            
        </div>
    </div>
    <%- include('common/footer.ejs') %>
</body>
</html>