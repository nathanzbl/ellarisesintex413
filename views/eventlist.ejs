<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Ella Rises | Events</title>

<style>
    body {
        background-color: #faf6ef;
        font-family: Arial, sans-serif;
        margin: 0;
    }

    header {
        background-color: #f7cfd5;
        padding: 20px 50px;
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    nav a {
        margin: 0 20px;
        padding: 8px 18px;
        border-radius: 20px;
        background-color: #ffffffcc;
        font-weight: bold;
        color: #333;
        text-decoration: none;
    }

    .donate-btn {
        background-color: #9bbccc;
        color: white !important;
    }

    .page-title {
        text-align: center;
        margin: 35px 0;
        font-size: 42px;
        font-family: 'Georgia', serif;
        color: #333;
    }

    .event-grid {
        width: 80%;
        margin: auto;
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(290px, 1fr));
        gap: 25px;
        padding-bottom: 50px;
    }

    .event-card {
        background: white;
        padding: 25px;
        border-radius: 20px;
        text-decoration: none;
        color: #333;
        box-shadow: 0 10px 25px rgba(0,0,0,0.08);
        border-top: 8px solid #f7cfd5;
        transition: transform 0.2s ease, box-shadow 0.2s ease;
    }

    .event-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 15px 30px rgba(0,0,0,0.12);
    }

    .event-card h3 {
        margin-top: 0;
        font-size: 22px;
        color: #333;
    }

    .event-card p {
        font-size: 15px;
        color: #666;
        margin-top: 8px;
    }
</style>
</head>

<body>

<header>
    <img src="/images/EllaRisesLogo.png.avif" height="60">
    <nav>
        <a href="/">Home</a>
        <a href="/events">Events</a>
        <a href="/participants">Participants</a>
        <a href="/milestones">Milestones</a>
        <a href="/surveys">Surveys</a>
        <a href="/donations" class="donate-btn">Donations</a>
    </nav>
</header>

<h1 class="page-title">Select an Event Type</h1>

<div class="event-grid">
    <% eventDefs.forEach(def => { %>
        <a class="event-card" href="/events/<%= def.eventdefid %>">
            <h3><%= def.eventname %></h3>
            <p><%= def.eventdescription %></p>
        </a>
    <% }) %>
</div>

</body>
</html>
