<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Add Event | Ella Rises</title>

<style>
    :root {
        --er-blue:     #99B7C6;
        --er-lavender: #978EC4;
        --er-cream:    #F9F5EA;
        --er-blush:    #FFD8D1;
        --er-pink:     #F9AFB1;
        --er-sage:     #9AB59D;
        --er-charcoal: #3A3F3B;
        --er-peach:    #F4B092;
        --er-rose:     #CE325B;
    }

    body {
        margin: 0;
        font-family: "Montserrat", sans-serif;
        background: var(--er-cream);
        color: var(--er-charcoal);
    }

    .page-title {
        text-align: center;
        font-size: 2.7rem;
        margin-top: 2.5rem;
        margin-bottom: 1.5rem;
        font-family: "DM Serif Display", serif;
        color: var(--er-charcoal);
        letter-spacing: 0.02em;
    }

    .container {
        width: 70%;
        margin: 0 auto;
        background: white;
        padding: 3rem;
        border-radius: 1.5rem;
        border-top: 10px solid var(--er-pink);
        box-shadow: 0 12px 28px rgba(0,0,0,0.12);
    }

    label {
        font-weight: 600;
        font-size: 0.95rem;
        margin-top: 1.4rem;
        display: block;
        color: var(--er-charcoal);
        letter-spacing: 0.04em;
    }

    input, textarea, select {
        width: 100%;
        padding: 14px;
        margin-top: 0.4rem;
        border-radius: 12px;
        border: 1px solid #ddd;
        font-size: 0.95rem;
        background: #fff;
    }

    textarea {
        resize: vertical;
    }

    button {
        margin-top: 2rem;
        width: 100%;
        padding: 1rem;
        background: var(--er-pink);
        color: white;
        border: none;
        border-radius: 12px;
        font-size: 1.1rem;
        font-weight: 600;
        cursor: pointer;
        box-shadow: 0 10px 25px rgba(249,175,177,0.4);
        transition: 0.2s ease;
        letter-spacing: 0.05em;
    }

    button:hover {
        background: var(--er-rose);
        box-shadow: 0 14px 32px rgba(206,50,91,0.35);
    }

    .error {
        text-align: center;
        color: var(--er-rose);
        font-weight: bold;
        margin-bottom: 1rem;
    }
</style>
</head>

<body>

<%- include('../navbar.ejs') %>

<h1 class="page-title">Add New Event</h1>

<div class="container">

    <% if (error_message) { %>
        <p class="error"><%= error_message %></p>
    <% } %>

    <form action="/events/add" method="POST">

        <label>Event Name</label>
        <input type="text" name="eventname" required>

        <label>Description</label>
        <textarea name="eventdescription" rows="3" required></textarea>

        <label>Event Type</label>
        <select name="eventtype" required>
            <option value="Service">Service</option>
            <option value="Workshop">Workshop</option>
            <option value="Activity">Activity</option>
            <option value="Other">Other</option>
        </select>

        <label>Recurrence Pattern</label>
        <select name="eventrecurrencepattern" required>
            <option value="None">None</option>
            <option value="Monthly">Monthly</option>
            <option value="Weekly">Weekly</option>
            <option value="Yearly">Yearly</option>
        </select>

        <label>Start Date & Time</label>
        <input type="datetime-local" name="eventdatetimestart" required>

        <label>End Date & Time</label>
        <input type="datetime-local" name="eventdatetimeend" required>

        <label>Location</label>
        <input type="text" name="eventlocation" required>

        <label>Capacity</label>
        <input type="number" name="eventcapacity" required>

        <button type="submit">Save Event</button>

    </form>
</div>

</body>
</html>

