<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title data-i18n-key="doc_title_events">Ella Rises | Events</title>

<style>
    :root {
        --er-blue:     #99B7C6;
        --er-lavender: #978EC4;
        --er-cream:    #F9F5EA;
        --er-blush:    #FFD8D1;
        --er-pink:     #F9AFB1;
        --er-sage:     #9AB59D;
        --er-charcoal: #3A3F3B;
        --er-peach:    #F4B092;
        --er-rose:     #CE325B;
    }

    body {
        margin: 0;
        background: var(--er-cream);
        font-family: "Montserrat", sans-serif;
        color: var(--er-charcoal);
    }

    .events-image-wrapper {
        width: 100%;
        display: flex;
        justify-content: center;
        margin-top: 1.5rem;
    }

    .events-image {
        width: 50%;          /* adjust width */
        height: 250px;       /* smaller height */
        object-fit: cover;   /* keeps image from stretching */
        border-radius: 0;    /* removes rounded corners */
        display: block;
    }

    .page-title {
        text-align: center;
        font-size: 2.5rem;
        margin-top: 2rem;
        font-family: "DM Serif Display", serif;
    }

    .event-grid {
        width: 85%;
        margin: 2rem auto;
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
        gap: 25px;
    }

    @media only screen and (max-width: 768px) {
        .event-grid {
            grid-template-columns: 1fr !important;
            width: 92% !important;
        }
    }

    .event-card {
        background: #fff;
        padding: 2rem;
        border-top: 6px solid var(--er-pink);
        box-shadow: 0 10px 25px rgba(0,0,0,0.08);
        transition: all 0.3s ease;
        text-decoration: none;
        color: var(--er-charcoal);
        
        /* No rounded corners */
        border-radius: 0;
        
        /* Needed for smooth expansion */
        position: relative;
        overflow: hidden;
    }

    /*  Expand downward on hover */
    .event-card:hover {
        padding-bottom: 5rem;
        box-shadow: 0 18px 38px rgba(0,0,0,0.15);
    }

    .event-name {
        font-size: 1.4rem;
        font-weight: 600;
        margin-bottom: .5rem;
        letter-spacing: 0.04em;
    }

    .event-description {
        color: rgba(58,63,59,0.75);
        font-size: .9rem;
        line-height: 1.5rem;
        min-height: 3.4rem;
    }

    /*  Action buttons reveal from BELOW the card */
    .hover-actions {
        position: absolute;
        left: 0;
        right: 0;
        bottom: -120px; /* Hidden further down to accommodate longer Spanish text */
        padding: 1rem;
        text-align: center;
        transition: bottom 0.3s ease;
        background: white;
        border-top: 2px solid var(--er-blush);
    }

    /* Move buttons upward into view */
    .event-card:hover .hover-actions {
        bottom: 0;
    }

    .btn-action {
        padding: .7rem 1rem;
        border-radius: 0; /* No rounded corners */
        border: none;
        background: var(--er-pink);
        color: #fff;
        font-weight: 600;
        letter-spacing: 0.05em;
        font-size: .85rem; /* Slightly smaller to fit longer Spanish text */
        text-decoration: none;
        margin: .3rem .2rem;
        display: inline-block;
        transition: all 0.2s ease;
        box-shadow: 0 2px 8px rgba(249, 175, 177, 0.3);
        white-space: nowrap; /* Prevent text wrapping */
    }

    .btn-action:hover {
        background: var(--er-rose);
        transform: translateY(-2px);
        box-shadow: 0 4px 12px rgba(206, 50, 91, 0.4);
    }

    .event-img {
    width: 100%;
    height: 180px;
    object-fit: cover;
    display: block;
    margin-bottom: 1rem;
}

/* Mobile Responsive */
@media (max-width: 768px) {
    body {
        font-size: 18px;
    }

    .page-title {
        font-size: 2.2rem;
        margin-top: 1.5rem;
        padding: 0 1rem;
        font-weight: 600;
    }

    .event-grid {
        width: 92%;
        grid-template-columns: 1fr;
        gap: 1.5rem;
        margin: 1.5rem auto;
    }

    .event-card {
        padding: 1.8rem;
        width: 100%;
        max-width: 100%;
        min-width: 280px;
    }

    .event-card:hover {
        padding-bottom: 6.5rem;
    }

    .event-name {
        font-size: 1.3rem;
        font-weight: 600;
    }

    .event-description {
        font-size: 0.95rem;
        line-height: 1.6;
    }

    .hover-actions {
        bottom: -150px;
        padding: 1.2rem 0.8rem;
    }

    .btn-action {
        padding: 1rem 1.4rem;
        font-size: 1rem;
        margin: 0.4rem;
        min-height: 48px;
        min-width: 120px;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        font-weight: 600;
    }

    .event-img {
        height: 220px;
    }
}



</style>
</head>

<body>

<%- include('../common/navbar.ejs') %>

<%- include('../spanishtranslationstorage.ejs') %>

<h1 class="page-title" data-i18n-key="events_page_title">Events</h1>


<div class="event-grid">
    <% eventDefs.forEach(def => { %>
        <div class="event-card">
            <% if (def.eventimage) { %>
                <img src="/images/<%= def.eventimage %>" alt="Event image" class="event-img">
            <% } else { %>
                <img src="/images/default-event.jpg" class="event-img">
            <% } %>
            <div class="event-name"><%= def.eventname %></div>
            <div class="event-description"><%= def.eventdescription %></div>

            <!-- Expand-down Actions -->
            <div class="hover-actions">
                <a href="/eventspublic/detail/<%= def.eventdefid %>" class="btn-action" data-i18n-key="events_btn_more_info">More Info</a>
                <a href="/rsvp/<%= def.eventdefid %>" class="btn-action" data-i18n-key="events_btn_rsvp">RSVP</a>
            </div>

        </div>
    <% }) %>
</div>
<%- include('../common/footer.ejs') %>

</body>
</html>
