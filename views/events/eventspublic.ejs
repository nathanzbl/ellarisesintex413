<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <title>Upcoming Events | Ella Rises</title>

    <style>
        body {
            margin: 0;
            font-family: 'Arial', sans-serif;
            background-color: #faf6ef;
        }

        header {
            background-color: #f7cfd5;
            padding: 20px 50px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-bottom: 1px solid #e8b9c6;
        }

        nav a {
            margin: 0 20px;
            text-decoration: none;
            color: #333;
            padding: 8px 18px;
            border-radius: 20px;
            background-color: #ffffffcc;
            font-weight: bold;
            font-size: 15px;
        }

        .donate-btn {
            background-color: #9bbccc;
            color: white !important;
        }

        .page-title {
            text-align: center;
            margin-top: 35px;
            font-size: 42px;
            font-family: 'Georgia', serif;
            color: #333;
            letter-spacing: 2px;
        }

        .container {
            width: 60%;
            margin: 40px auto;
            background: white;
            border-radius: 20px;
            padding: 50px;
            border-top: 10px solid #f7cfd5;
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.08);
        }

        .event-name {
            text-align: center;
            font-size: 28px;
            font-family: 'Georgia', serif;
            margin-bottom: 10px;
        }

        .event-meta {
            text-align: center;
            color: #555;
            font-size: 16px;
            margin-bottom: 30px;
        }

        .pink-btn {
            display: block;
            width: 180px;
            margin: 25px auto;
            padding: 14px;
            background-color: #e7a2b8;
            color: white;
            text-align: center;
            border-radius: 30px;
            text-decoration: none;
            font-weight: bold;
            font-size: 16px;
        }

        .pink-btn:hover {
            background-color: #d88aa2;
        }

        .detail-link {
            text-align: center;
            display: block;
            margin-bottom: 15px;
            text-decoration: underline;
            font-size: 17px;
            color: #333;
        }
    </style>
</head>

<body>

<%- include('../common/navbar.ejs') %>

<h1 class="page-title">Upcoming Events</h1>

<% if (nextEvent) { %>
<div class="container">
    
    <!-- Event Name -->
    <h2 class="event-name"><%= nextEvent.eventname %></h2>

    <!-- Event Date + Location -->
    <div class="event-meta">
        <%= nextEvent.eventdatetimestart.toLocaleString() %> • 
        <strong><%= nextEvent.eventlocation %></strong>
    </div>

    <!-- More Info -->
    <a href="/events/detail/<%= nextEvent.eventid %>" class="detail-link">
        More Info
    </a>

    <!-- RSVP Button -->
    <a href="/events/rsvp/<%= nextEvent.eventid %>" class="pink-btn">
        RSVP
    </a>
</div>

<% } else { %>
<p style="text-align:center; font-size:18px; color:#666;">No upcoming events.</p>
<% } %>
<%- include('../common/footer.ejs') %>
</body>
</html>
