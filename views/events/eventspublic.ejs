<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Ella Rises | Events</title>

<style>
    :root {
        --er-blue:     #99B7C6;
        --er-lavender: #978EC4;
        --er-cream:    #F9F5EA;
        --er-blush:    #FFD8D1;
        --er-pink:     #F9AFB1;
        --er-sage:     #9AB59D;
        --er-charcoal: #3A3F3B;
        --er-peach:    #F4B092;
        --er-rose:     #CE325B;
    }

    body {
        margin: 0;
        background: var(--er-cream);
        font-family: "Montserrat", sans-serif;
        color: var(--er-charcoal);
    }

    .events-image-wrapper {
        width: 100%;
        display: flex;
        justify-content: center;
        margin-top: 1.5rem;
    }

    .events-image {
        width: 50%;          /* adjust width */
        height: 250px;       /* smaller height */
        object-fit: cover;   /* keeps image from stretching */
        border-radius: 0;    /* removes rounded corners */
        display: block;
    }

    .page-title {
        text-align: center;
        font-size: 2.5rem;
        margin-top: 2rem;
        font-family: "DM Serif Display", serif;
    }

    .event-grid {
        width: 85%;
        margin: 2rem auto;
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
        gap: 25px;
    }

    .event-card {
        background: #fff;
        padding: 2rem;
        border-top: 6px solid var(--er-pink);
        box-shadow: 0 10px 25px rgba(0,0,0,0.08);
        transition: all 0.3s ease;
        text-decoration: none;
        color: var(--er-charcoal);
        
        /* No rounded corners */
        border-radius: 0;
        
        /* Needed for smooth expansion */
        position: relative;
        overflow: hidden;
    }

    /*  Expand downward on hover */
    .event-card:hover {
        padding-bottom: 5rem;
        box-shadow: 0 18px 38px rgba(0,0,0,0.15);
    }

    .event-name {
        font-size: 1.4rem;
        font-weight: 600;
        margin-bottom: .5rem;
        letter-spacing: 0.04em;
    }

    .event-description {
        color: rgba(58,63,59,0.75);
        font-size: .9rem;
        line-height: 1.5rem;
        min-height: 3.4rem;
    }

    /*  Action buttons reveal from BELOW the card */
    .hover-actions {
        position: absolute;
        left: 0;
        right: 0;
        bottom: -80px; /* Hidden at first */
        padding: 1rem;
        text-align: center;
        transition: bottom 0.3s ease;
        background: white;
        border-top: 2px solid var(--er-blush);
    }

    /* Move buttons upward into view */
    .event-card:hover .hover-actions {
        bottom: 0;
    }

    .btn-action {
        padding: .7rem 1.3rem;
        border-radius: 0; /* No rounded corners */
        border: none;
        background: var(--er-pink);
        color: #fff;
        font-weight: 600;
        letter-spacing: 0.07em;
        font-size: .9rem;
        text-decoration: none;
        margin: .4rem 0;
        display: inline-block;
        transition: background 0.2s ease;
    }

    .btn-action:hover {
        background: var(--er-rose);
    }

    .event-img {
    width: 100%;
    height: 180px;
    object-fit: cover;
    display: block;
    margin-bottom: 1rem;
}


</style>
</head>

<body>

<%- include('../common/navbar.ejs') %>

<h1 class="page-title">Events</h1>


<div class="event-grid">
    <% eventDefs.forEach(def => { %>
        <div class="event-card">
            <% if (def.eventimage) { %>
                <img src="/images/<%= def.eventimage %>" alt="Event image" class="event-img">
            <% } else { %>
                <img src="/images/default-event.jpg" class="event-img">
            <% } %>
            <div class="event-name"><%= def.eventname %></div>
            <div class="event-description"><%= def.eventdescription %></div>

            <!-- Expand-down Actions -->
            <div class="hover-actions">
                <a href="/eventspublic/detail/<%= def.eventdefid %>" class="btn-action">More Info</a>
                <a href="/rsvp/<%= def.eventdefid %>" class="btn-action">RSVP</a>
            </div>

        </div>
    <% }) %>
</div>
<%- include('../common/footer.ejs') %>

</body>
</html>
